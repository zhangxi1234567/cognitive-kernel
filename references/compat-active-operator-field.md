# Compatibility Boundary Note

This document no longer defines an operator field.

## Hard Rule

Do not expose operator candidates, operator neighborhoods, or operator competition surfaces as runtime-facing structures.

The package may preserve thinner behavior.
It must not expose an operator control plane.
