# Compatibility Layer Vs Native Runtime

The distinction remains simple.

- compatibility layer: thin boundary and bias surface
- native runtime: live host modulation without visible control packaging

## Final Rule

The project moves in the right direction only when the visible layer gets thinner.
