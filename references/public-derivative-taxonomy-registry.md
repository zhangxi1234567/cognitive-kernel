# Wu Boshi Public Derivative Taxonomy Registry

This file is a structured placeholder-registry for the public derivative-series lines:

- `五大类型`
- `六大类型`

Current public surfaces strongly confirm that the taxonomy exists, but not the exact canonical list.

## Registry Schema

For each eventual type entry, record:

1. `Public taxonomy name`
2. `Type label`
3. `Recognition cue`
4. `Default route`
5. `Common bait route`
6. `Boundary / witness role`
7. `Cheapest faithful seal`
8. `Cross-level note`
9. `Evidence status`

## What The Public Taxonomy Is Clearly Doing

Even before the exact list is known, the series is clearly training:

- topic compression
- route-before-derivation
- family recognition
- bait-route refusal

So the taxonomy already contributes to the skill library even while some labels remain unknown.

## Likely Type Families Already Suggested By Public Surface

These are not canonical final names.
They are working cluster slots inferred from repeated public wrappers:

- monotonicity / sign-control
- parameter boundary / witness
- construction-function
- mean-value / midpoint model
- shape / convexity / graph-behavior
- mother-function / over-standard-function routing

## Hard Rule

Do not present the inferred slots as the public official list.
Use them as integration slots only, until stronger evidence arrives.
