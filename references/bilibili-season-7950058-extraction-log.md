# Bilibili Season 7950058 Extraction Log

Source:

- short link: `https://b23.tv/KFZeObm`
- resolved BV entry: `BV1dWd1BfEuJ`

## Season Metadata

- Season title:
  - `高考数学`
- Season ID:
  - `7950058`
- Intro:
  - `降维打击`
- Uploader:
  - `hedanzhuangdiqiu`

## Current Extraction State

- collection metadata: `fetched`
- episode list: `fetched`
- direct subtitles: `not available`
- danmaku xml: `available via yt-dlp`
- current audio downloaded:
  - [高考数学降维打击 [BV1dWd1BfEuJ].m4a](c:/Users/21604/Desktop/niu/.omx/media/%E9%AB%98%E8%80%83%E6%95%B0%E5%AD%A6%E9%99%8D%E7%BB%B4%E6%89%93%E5%87%BB%20[BV1dWd1BfEuJ].m4a)
- season raw JSON snapshot:
  - [wu-boshi-bili-season-7950058.json](c:/Users/21604/Desktop/niu/.omx/wu-boshi-bili-season-7950058.json)
- episode info JSON snapshot:
  - [bv1dWd1BfEuJ-info.json](c:/Users/21604/Desktop/niu/.omx/bv1dWd1BfEuJ-info.json)

## Episode Index Snapshot

The season currently exposes these visible episodes from the fetched metadata:

1. `BV1sjd6BFEbg` | `高考数学` | `part=020` | `89:32`
2. `BV1dWd1BfEuJ` | `高考数学降维打击` | `part=001` | `107:16`
3. `BV1iWd1BfEwL` | `高考数学降维打击` | `part=003` | `94:06`
4. `BV1vWd1BfEBK` | `高考数学降维打击` | `part=002` | `91:10`
5. `BV1jMdUBqE4Z` | `高考数学降维打击` | `part=004` | `92:13`
6. `BV1zwdUBrEhV` | `高考数学降维打击` | `part=005` | `92:44`
7. `BV1JwdUBrETb` | `高考数学降维打击` | `part=006` | `94:39`
8. `BV1EwdUBrE65` | `高考数学降维打击` | `part=007` | `95:38`
9. `BV1FcdUBVEfN` | `高考数学降维打击` | `part=009` | `91:57`
10. `BV17NdUBzE73` | `高考数学降维打击` | `part=012` | `93:52`
11. `BV1BNdUBzE3E` | `高考数学降维打击` | `part=011` | `93:26`
12. `BV1EPdUBgE7y` | `高考数学降维打击` | `part=013` | `98:08`
13. `BV17PdUBgEZA` | `高考数学降维打击` | `part=014` | `96:21`
14. `BV1eVdUBPE5j` | `高考数学降维打击` | `part=015` | `91:36`
15. `BV1VVdUBPEoZ` | `高考数学降维打击` | `part=016` | `110:01`
16. `BV15VdUBPETA` | `高考数学降维打击` | `part=022` | `95:03`
17. `BV1eVdUBPEKD` | `高考数学降维打击` | `part=025` | `104:01`
18. `BV1eVdUBPEGj` | `高考数学降维打击` | `part=026` | `89:36`
19. `BV1h7dUBhEYk` | `高考数学降维打击` | `part=028` | `92:24`
20. `BV137dUBhEiF` | `高考数学降维打击` | `part=027` | `91:21`
21. `BV137dUBhE6A` | `高考数学降维打击` | `part=029` | `98:19`

## Mechanism-First Extraction Rule

This season should not be mined as:

- one long topic playlist
- a flat list of solved questions
- a chapter-by-chapter transcript dump

It should be mined as:

- repeated trigger signals
- repeated route-compression moves
- repeated superfamily behavior
- repeated seed evidence for existing primitives

## Current Working Hypothesis

Given the season title and the already extracted PDF corpus, this season is likely to strengthen:

- `Area To Line Readout`
- `Projection Readout`
- `Container To Section`
- `Definition As Direct Readout`
- `Symmetry As Variable Killer`
- `Boundary As Route Finder`
- `Model Calling Before Derivation`
- `Special-Value Probing`
- `Function Archetype Matching`

## Next Step

1. strengthen direct non-season anchors for the controllers that are still weaker outside this season, especially `Projection Readout`, `Boundary As Route Finder`, and `Special-Value Probing` / `Guess Then Verify`
2. treat `Draw-First Decoding`, `Function Archetype Matching`, `Model Calling Before Derivation`, and `Count-Do-Not-Derive` as already cross-lane-backed rather than season-local discoveries
3. only open higher-layer restructures when the weaker-anchored controllers also recur outside season `7950058` with comparably direct evidence

## First 20-Minute Extraction Notes (`BV1dWd1BfEuJ`)

Working source:

- [BV1dWd1BfEuJ-first20min.txt](c:/Users/21604/Desktop/niu/.omx/transcripts/BV1dWd1BfEuJ-first20min.txt)

### Observed mechanism signals

1. `不完美条件下先排错再锁定`
   - the speaker explicitly teaches that new-style questions may not feel 100 percent determinate at first
   - the route is:
     - remove obviously impossible options
     - accept bounded uncertainty
     - choose the most structurally reasonable remaining answer
   - this is not yet promoted as a primitive
   - current status:
     - `candidate mechanism`

2. `Special-Value Probing`
   - the speaker explicitly says “找特例”
   - on the second question he chooses a simple assignment (`B=3`, `A=1` in the rough transcript) to collapse the route quickly
   - current status:
     - `confirmed as season-level seed evidence`

3. `Draw First / geometry externalization`
   - around the later part of the first 20 minutes he repeatedly says “都是画图”
   - especially when moving to line-plane geometry, the route becomes “draw first, then eliminate false relations”
   - current status:
     - `confirmed as season-level support for visual externalization`

4. `Named micro-method routing`
   - the transcript references a named sequence method (`数...的方法` in noisy ASR)
   - this suggests the season uses named compact models repeatedly rather than raw derivation
   - current status:
     - `useful-adjacent evidence for Model Calling Before Derivation`

### Immediate extraction judgment

The first 20 minutes already reinforce:

- `Special-Value Probing`
- `Draw-First Decoding`
- `Model Calling Before Derivation` (light support)

They also suggest a new possible future card around:

- handling bounded ambiguity by structural elimination instead of waiting for perfect certainty

## 20-40 Minute Extraction Notes (`BV1dWd1BfEuJ`)

Working source:

- [BV1dWd1BfEuJ-20to40min.txt](c:/Users/21604/Desktop/niu/.omx/transcripts/BV1dWd1BfEuJ-20to40min.txt)

### Observed mechanism signals

1. `Draw-First Decoding` strengthened again
   - the speaker explicitly repeats:
     - `所有题都画图`
     - `图才能看得透`
     - `数学都是图`
   - this is much stronger than a one-off geometry suggestion
   - it is being taught as a cross-topic cognitive posture
   - current status:
     - `confirmed strong season-level support`

2. `Count-Do-Not-Derive`
   - on constrained arrangement / grouping material, the route is explicitly:
     - draw
     - place the constrained actors first
     - count directly
   - current status:
     - `confirmed as season-level seed evidence`

3. `Most-Constrained-First`
   - the arrangement route places the problematic / restricted people first, then lets the rest fill freely
   - current status:
     - `confirmed useful support`

4. `Definition As Direct Readout`
   - on the new-definition set problem, the speaker says the most important thing is not computation but language understanding
   - then immediately routes into drawing the structure of the definition
   - current status:
     - `confirmed season-level seed evidence`

### Immediate extraction judgment

The first 40 minutes already reinforce:

- `Draw-First Decoding`
- `Count-Do-Not-Derive`
- `Most-Constrained-First`
- `Definition As Direct Readout`

And they reinforce one meta-principle very strongly:

- `all difficult statements should be externalized before deep solving`

## 40-60 Minute Extraction Notes (`BV1dWd1BfEuJ`)

Working source:

- [BV1dWd1BfEuJ-40to60min.txt](c:/Users/21604/Desktop/niu/.omx/transcripts/BV1dWd1BfEuJ-40to60min.txt)

### Observed mechanism signals

1. `Definition As Direct Readout` strengthened again
   - the speaker explicitly says new-definition problems are first `阅读理解题`
   - the route is:
     - read the definition carefully
     - translate terms into object roles
     - draw the structure
     - only then enumerate
   - current status:
     - `confirmed strong season-level support`

2. `Draw-First Decoding` becomes cross-subject doctrine
   - the speaker repeats stronger doctrine-like language:
     - `数学都是图`
     - `画图, 列举出来也是一种图`
   - this is no longer local geometry advice; it is a universal representation principle
   - current status:
     - `confirmed strong season-level support`

3. `Count-Do-Not-Derive` strengthened
   - on the `Standard三元系`-style definition problem, the route is direct structured enumeration
   - current status:
     - `confirmed strong season-level support`

4. `Probability / tournament process should be drawn as state flow`
   - the transcript moves to a four-person cyclic competition and handles it by drawing the process flow
   - this strongly suggests a route family around:
     - process -> state graph / tree
   - current status:
     - `candidate mechanism support`

### Immediate extraction judgment

The first 60 minutes now strongly reinforce:

- `Definition As Direct Readout`
- `Draw-First Decoding`
- `Count-Do-Not-Derive`

They also suggest a future bridge candidate:

- dynamic process -> state-flow readout

## 60-80 Minute Extraction Notes (`BV1dWd1BfEuJ`)

Working source:

- [BV1dWd1BfEuJ-60to80min.txt](c:/Users/21604/Desktop/niu/.omx/transcripts/BV1dWd1BfEuJ-60to80min.txt)

### Observed mechanism signals

1. `Special-Value Probing` strengthened again
   - the speaker explicitly says `因为我找特例`
   - he chooses angle/value special cases to collapse an otherwise annoying derivation
   - current status:
     - `confirmed strong season-level support`

2. `Function Archetype Matching` support
   - on abstract function material, the transcript repeatedly pushes the route toward a standard compact family behavior (`周期为六`, `像一个常见函数结构`)
   - current status:
     - `useful-adjacent to confirmed`

3. `Draw-First + target-only elimination`
   - in geometry and graph-like judgment sections, the route is:
     - draw
     - eliminate impossible options
     - keep only the few structurally viable ones
   - current status:
     - `confirmed support`

4. `bounded-confidence solving`
   - the speaker repeatedly teaches not to overprotect against tiny uncertainty when the expected-value gain is clearly positive
   - this is not yet a primitive, but it is becoming a recurring decision heuristic
   - current status:
     - `candidate decision heuristic`

### Immediate extraction judgment

The first 80 minutes now strongly reinforce:

- `Special-Value Probing`
- `Definition As Direct Readout`
- `Draw-First Decoding`
- `Count-Do-Not-Derive`

And they add more support to:

- `Function Archetype Matching`

## 80-107 Minute Extraction Notes (`BV1dWd1BfEuJ`)

Working source:

- [BV1dWd1BfEuJ-80to107min.txt](c:/Users/21604/Desktop/niu/.omx/transcripts/BV1dWd1BfEuJ-80to107min.txt)

### Observed mechanism signals

1. `Special-Value Probing` strengthened further
   - the speaker explicitly says `条件不足就是找特例`
   - this is now strong evidence that special-case probing is one of the default route selectors in the season
   - current status:
     - `confirmed strong season-level support`

2. `Function Archetype Matching` strengthened
   - on abstract function material the route keeps collapsing toward a compact recognizable behavior family (`周期`, `标准形`, `像一个常见结构`)
   - current status:
     - `confirmed season-level support`

3. `Projection / direct readout in space geometry`
   - later spatial-geometry discussion repeatedly uses direct coordinate / normal-vector readout instead of rebuilding the whole solid
   - current status:
     - `confirmed useful support for Projection Readout`

4. `Draw-First / externalize-first` remains dominant
   - even in later sections, the route repeatedly begins by drawing, stripping, or translating the object into a cleaner visible controller
   - current status:
     - `confirmed dominant season-level doctrine`

### Episode-Level Judgment For `BV1dWd1BfEuJ`

After full-episode extraction, this episode strongly reinforces:

- `Draw-First Decoding`
- `Definition As Direct Readout`
- `Count-Do-Not-Derive`
- `Special-Value Probing`

And gives clear additional support to:

- `Most-Constrained-First`
- `Function Archetype Matching`
- `Model Calling Before Derivation`
- `Projection Readout`

## First 20-Minute Extraction Notes (`BV1vWd1BfEBK`)

Working source:

- [BV1vWd1BfEBK-first20min.txt](c:/Users/21604/Desktop/niu/.omx/transcripts/BV1vWd1BfEBK-first20min.txt)

### Observed mechanism signals

1. `Draw-First Decoding` remains the lead doctrine
   - the speaker immediately reaffirms:
     - difficult problems should be drawn
     - geometry/analytic problems become manageable after diagramming
   - current status:
     - `confirmed strong season-level support`

2. `Guess Then Verify`
   - the opening explicitly says:
     - if you can guess the answer, guess it first
     - then use the guessed target to avoid heavy derivation
   - current status:
     - `confirmed strong season-level support`

3. `Special-Value Probing`
   - the line-family / fixed-point material is attacked by setting special slopes such as `0`, `∞`, and equal cases
   - current status:
     - `confirmed strong season-level support`

4. `Boundary As Route Finder`
   - the episode repeatedly uses extreme/limiting parameter choices (`0`, `∞`, endpoint-like cases) as route-finders for a fixed-point claim
   - current status:
     - `confirmed useful support`

### Immediate extraction judgment

The first 20 minutes of `BV1vWd1BfEBK` strongly reinforce:

- `Draw-First Decoding`
- `Guess Then Verify`
- `Special-Value Probing`
- `Boundary As Route Finder`

## 20-40 Minute Extraction Notes (`BV1vWd1BfEBK`)

Working source:

- [BV1vWd1BfEBK-20to40min.txt](c:/Users/21604/Desktop/niu/.omx/transcripts/BV1vWd1BfEBK-20to40min.txt)

### Observed mechanism signals

1. `Guess Then Verify` strengthened
   - the transcript repeatedly says that because the answer or fixed point is already guessed, heavy derivation can be replaced by direct substitution / verification
   - current status:
     - `confirmed strong season-level support`

2. `Formula / invariant template as error-control`
   - not a new primitive yet, but there is a strong recurring tactic:
     - derive or remember a compact formula family once
     - then plug structure into it instead of recomputing from scratch
   - this currently supports:
     - `Model Calling Before Derivation`

3. `Boundary / impossible-case elimination`
   - the transcript repeatedly checks impossible perpendicular or endpoint cases to kill branches
   - current status:
     - `confirmed useful support for Boundary As Route Finder`

### Immediate extraction judgment

The first 40 minutes of `BV1vWd1BfEBK` strongly reinforce:

- `Guess Then Verify`
- `Boundary As Route Finder`
- `Model Calling Before Derivation`

## 40-60 Minute Extraction Notes (`BV1vWd1BfEBK`)

Working source:

- [BV1vWd1BfEBK-40to60min.txt](c:/Users/21604/Desktop/niu/.omx/transcripts/BV1vWd1BfEBK-40to60min.txt)

### Observed mechanism signals

1. `Symmetry As Variable Killer` support
   - the transcript explicitly advises rewriting asymmetric target forms into symmetric forms before using the formula system
   - current status:
     - `confirmed useful support`

2. `Model Calling Before Derivation` strengthened
   - the speaker keeps saying the large family of conic methods is "basically the same formulas" and should be called as a compact system
   - this is a very strong "call the toolkit/model first, then plug conditions" behavior
   - current status:
     - `confirmed strong season-level support`

3. `Condition translation before solving`
   - many conditions are explicitly translated into:
     - equal distances
     - opposite signs
     - midpoint / center averages
     - line equations
   - this is strong support for the relation-first stack
   - current status:
     - `confirmed strong season-level support`

### Immediate extraction judgment

The first 60 minutes of `BV1vWd1BfEBK` now strongly reinforce:

- `Guess Then Verify`
- `Model Calling Before Derivation`
- `Definition / relation-first translation`

And add more support to:

- `Symmetry As Variable Killer`

## 60-80 Minute Extraction Notes (`BV1vWd1BfEBK`)

Working source:

- [BV1vWd1BfEBK-60to80min.txt](c:/Users/21604/Desktop/niu/.omx/transcripts/BV1vWd1BfEBK-60to80min.txt)

### Observed mechanism signals

1. `Projection / direct readout` support
   - the transcript repeatedly collapses geometry relations into direct controller readouts instead of carrying the whole object

2. `Condition-insufficient -> special case`
   - the route again uses “条件不足” as a signal to call a special-case / witness-value method

3. `Function archetype / explicit-form guess`
   - abstract function conditions are again compressed into a simple family candidate before long local work

### Current status

- transcript generated
- seed landing still in progress

## 80-91 Minute Extraction Notes (`BV1vWd1BfEBK`)

Working source:

- [BV1vWd1BfEBK-80to91min.txt](c:/Users/21604/Desktop/niu/.omx/transcripts/BV1vWd1BfEBK-80to91min.txt)

### Observed mechanism signals

1. `Function Archetype Matching` strengthened
   - abstract function content is again compressed toward simple explicit family candidates before long raw manipulation

2. `Draw-First Decoding` remains dominant
   - the transcript explicitly treats many later items as things that should be drawn first and solved from the picture

3. `Count-Do-Not-Derive` support
   - probability/counting material continues to be handled by direct case counting and structural reading rather than formula-first processing

4. `Symmetry As Variable Killer` support
   - several quantity relations are collapsed by symmetry around a midpoint or balanced center

### Episode-Level Judgment For `BV1vWd1BfEBK`

After full-episode extraction, this episode strongly reinforces:

- `Draw-First Decoding`
- `Guess Then Verify`
- `Special-Value Probing`
- `Boundary As Route Finder`
- `Model Calling Before Derivation`

And gives additional support to:

- `Function Archetype Matching`
- `Count-Do-Not-Derive`
- `Symmetry As Variable Killer`

## First 20-Minute Extraction Notes (`BV1iWd1BfEwL`)

Working source:

- [BV1iWd1BfEwL-first20min.txt](c:/Users/21604/Desktop/niu/.omx/transcripts/BV1iWd1BfEwL-first20min.txt)

### Observed mechanism signals

1. `dynamic state-flow reading` support
   - the opening repeatedly frames a process-style problem through start state, later comparison, and branching status

2. `Guess Then Verify`
   - the transcript again stresses that once the route shape or answer is guessed, later work shrinks dramatically

3. `Boundary / regime split reading`
   - a noticeable amount of discussion is spent on when parameter regimes are open/closed and how to judge the special boundary case

### Immediate extraction judgment

The first 20 minutes of `BV1iWd1BfEwL` reinforce:

- `Guess Then Verify`
- `Boundary As Route Finder`

And provide useful adjacent support to:

- process/state-flow modeling

## 20-40 Minute Extraction Notes (`BV1iWd1BfEwL`)

Working source:

- [BV1iWd1BfEwL-20to40min.txt](c:/Users/21604/Desktop/niu/.omx/transcripts/BV1iWd1BfEwL-20to40min.txt)

### Observed mechanism signals

1. `Function Archetype Matching` strengthened
   - the transcript explicitly routes repeated function values into known prototype triangles/families instead of local brute force

2. `Model Calling Before Derivation` strengthened
   - the route repeatedly says: first identify the geometry/function template, then use the shared formula system

3. `Guess Then Verify` strengthened again
   - the transcript explicitly says that quantities like fixed value / fixed point / range should be guessed by a good special case first

### Immediate extraction judgment

The first 40 minutes of `BV1iWd1BfEwL` strongly reinforce:

- `Function Archetype Matching`
- `Model Calling Before Derivation`
- `Guess Then Verify`

## 40-60 Minute Extraction Notes (`BV1iWd1BfEwL`)

Working source:

- [BV1iWd1BfEwL-40to60min.txt](c:/Users/21604/Desktop/niu/.omx/transcripts/BV1iWd1BfEwL-40to60min.txt)

### Observed mechanism signals

1. `Special-Value Probing` remains dominant
   - the transcript keeps repeating that these questions can be done by finding a special case first

2. `Model Calling Before Derivation` strengthened
   - multiple problems are treated as “same family / same toolkit” with quick switching between ellipse, hyperbola, and parabola formula systems

3. `Boundary / infinity-point probing`
   - the transcript repeatedly uses idealized extreme positions like tangent/vertex/infinite-distance style points to read a fixed quantity

### Immediate extraction judgment

The first 60 minutes of `BV1iWd1BfEwL` strongly reinforce:

- `Special-Value Probing`
- `Model Calling Before Derivation`
- `Boundary As Route Finder`

## 60-80 Minute Extraction Notes (`BV1iWd1BfEwL`)

Working source:

- [BV1iWd1BfEwL-60to80min.txt](c:/Users/21604/Desktop/niu/.omx/transcripts/BV1iWd1BfEwL-60to80min.txt)

### Observed mechanism signals

1. `Projection / controller readout` strengthened
   - the transcript repeatedly turns geometry into controller quantities like center distance, projection, or one reduced readout

2. `Draw-First Decoding` remains dominant
   - the transcript repeatedly insists that once the picture is drawn, the answer “just comes out”

3. `Boundary / infinity-point probing`
   - one route explicitly uses the point at infinity / limiting line configuration to lock a fixed value

### Immediate extraction judgment

The first 80 minutes of `BV1iWd1BfEwL` now strongly reinforce:

- `Draw-First Decoding`
- `Projection Readout`
- `Boundary As Route Finder`

## 80-94 Minute Extraction Notes (`BV1iWd1BfEwL`)

Working source:

- [BV1iWd1BfEwL-80to94min.txt](c:/Users/21604/Desktop/niu/.omx/transcripts/BV1iWd1BfEwL-80to94min.txt)

### Observed mechanism signals

1. `special point catalogue` support
   - the transcript explicitly teaches a reusable list of special points: vertical/horizontal, zero/infinity, intersection, infinity point, symmetry point

2. `Boundary As Route Finder` strengthened
   - again the route repeatedly uses point at infinity, degenerate intersection, or limiting position to read a fixed point/line

3. `Model Calling Before Derivation` strengthened
   - the transcript repeatedly says many contest/high-school conic tasks are the same body of problems once you know the catalog of special points and the associated template

### Episode-Level Judgment For `BV1iWd1BfEwL`

After full-episode extraction, this episode strongly reinforces:

- `Boundary As Route Finder`
- `Projection Readout`
- `Model Calling Before Derivation`
- `Function Archetype Matching`

## First 20-Minute Extraction Notes (`BV1jMdUBqE4Z`)

Working source:

- [BV1jMdUBqE4Z-first20min.txt](c:/Users/21604/Desktop/niu/.omx/transcripts/BV1jMdUBqE4Z-first20min.txt)

### Observed mechanism signals

1. `Taylor/expansion as hidden engine` support
   - the transcript explicitly explains that many derivative/continuity judgments are secretly controlled by expansion-level reasoning

2. `Guess Then Verify` support
   - the transcript keeps stressing that once the answer form is guessed, later work becomes error-control rather than blind search

3. `process/physics metaphor for derivatives`
   - the transcript again maps derivatives into position/velocity/acceleration language

### Immediate extraction judgment

The first 20 minutes of `BV1jMdUBqE4Z` reinforce:

- `Guess Then Verify`
- `Math-Physics Bridge`
- expansion-based local route compression

## 20-40 Minute Extraction Notes (`BV1jMdUBqE4Z`)

Working source:

- [BV1jMdUBqE4Z-20to40min.txt](c:/Users/21604/Desktop/niu/.omx/transcripts/BV1jMdUBqE4Z-20to40min.txt)

### Observed mechanism signals

1. `Special-Value Probing` support
   - repeated direct advice: many hard questions can be done by `找特例`

2. `Boundary As Route Finder` support
   - special points such as vertical/horizontal, zero, infinity, intersection, and symmetry points are treated as a reusable route catalogue

3. `Model Calling Before Derivation` support
   - the transcript repeatedly says many tasks become easy once recognized as the same conic/toolkit pattern

### Immediate extraction judgment

The first 40 minutes of `BV1jMdUBqE4Z` reinforce:

- `Special-Value Probing`
- `Boundary As Route Finder`
- `Model Calling Before Derivation`

## 40-60 Minute Extraction Notes (`BV1jMdUBqE4Z`)

Working source:

- [BV1jMdUBqE4Z-40to60min.txt](c:/Users/21604/Desktop/niu/.omx/transcripts/BV1jMdUBqE4Z-40to60min.txt)

### Observed mechanism signals

1. `Taylor/expansion as hidden route` strengthened
   - the transcript explicitly says repeated differentiation is secretly equivalent to expansion-level local reasoning

2. `Seven-point / start-point style derivative heuristic` support
   - the transcript keeps treating “same start point / same position / compare speed/acceleration” as a reusable route family

3. `State/trajectory metaphor` support
   - derivative conditions are repeatedly recoded as motion and trajectory comparison

### Immediate extraction judgment

The first 60 minutes of `BV1jMdUBqE4Z` reinforce:

- `Math-Physics Bridge`
- expansion-based local route compression
- state/trajectory style derivative routing

## 60-80 Minute Extraction Notes (`BV1jMdUBqE4Z`)

Working source:

- [BV1jMdUBqE4Z-60to80min.txt](c:/Users/21604/Desktop/niu/.omx/transcripts/BV1jMdUBqE4Z-60to80min.txt)

### Observed mechanism signals

1. `Draw-First Decoding` remains dominant
2. `Boundary As Route Finder` strengthened
3. `Definition / seven-point route` support

### Immediate extraction judgment

The first 80 minutes of `BV1jMdUBqE4Z` reinforce:

- `Draw-First Decoding`
- `Boundary As Route Finder`
- derivative seven-point routing

## 80-92 Minute Extraction Notes (`BV1jMdUBqE4Z`)

Working source:

- [BV1jMdUBqE4Z-80to92min.txt](c:/Users/21604/Desktop/niu/.omx/transcripts/BV1jMdUBqE4Z-80to92min.txt)

### Observed mechanism signals

1. `Symmetry As Variable Killer` strengthened
   - the transcript explicitly frames symmetry as a way to eliminate many terms and simplify proof burden

2. `Boundary / special-point probing` strengthened
   - point catalogues and special positions are again used as the standard route-finding language

3. `derivative route taxonomy` support
   - the transcript explicitly organizes derivative tasks into several recurring route types

### Episode-Level Judgment For `BV1jMdUBqE4Z`

After full-episode extraction, this episode strongly reinforces:

- `Math-Physics Bridge`
- `Special-Value Probing`
- `Boundary As Route Finder`
- `Symmetry As Variable Killer`

And gives additional support to:

- derivative route taxonomy

## First 20-Minute Extraction Notes (`BV1zwdUBrEhV`)

Working source:

- [BV1zwdUBrEhV-first20min.txt](c:/Users/21604/Desktop/niu/.omx/transcripts/BV1zwdUBrEhV-first20min.txt)

### Observed mechanism signals

1. `Topic compression / hardest clusters` support
2. `Draw-First Decoding` dominant from the opening
3. `Guess Then Verify` support

### Immediate extraction judgment

The first 20 minutes of `BV1zwdUBrEhV` reinforce:

- `Draw-First Decoding`
- `Guess Then Verify`
- topic-compression framing

## 20-40 Minute Extraction Notes (`BV1zwdUBrEhV`)

Working source:

- [BV1zwdUBrEhV-20to40min.txt](c:/Users/21604/Desktop/niu/.omx/transcripts/BV1zwdUBrEhV-20to40min.txt)

### Observed mechanism signals

1. `Draw-First Decoding` remains dominant
2. `Feature-point / characteristic-point catalogue` support
3. `Function family graph composition` support

### Immediate extraction judgment

The first 40 minutes of `BV1zwdUBrEhV` reinforce:

- `Draw-First Decoding`
- `Function Archetype Matching`
- characteristic-point routing

## 40-60 Minute Extraction Notes (`BV1zwdUBrEhV`)

Working source:

- [BV1zwdUBrEhV-40to60min.txt](c:/Users/21604/Desktop/niu/.omx/transcripts/BV1zwdUBrEhV-40to60min.txt)

### Observed mechanism signals

1. `Function Archetype Matching` strengthened
   - the transcript explicitly groups transformed expressions into a small number of recurring function-family types

2. `Layered case discussion for relation-heavy equations`
   - repeated emphasis on a small but systematic discussion tree for quadratic-root / sign / valid-root cases

3. `Topic compression` support
   - the transcript repeatedly compresses a broad problem area into a few recurrent route classes

### Immediate extraction judgment

The first 60 minutes of `BV1zwdUBrEhV` reinforce:

- `Function Archetype Matching`
- `Topic Compression`
- structured case-discussion routing

## 60-80 Minute Extraction Notes (`BV1zwdUBrEhV`)

Working source:

- [BV1zwdUBrEhV-60to80min.txt](c:/Users/21604/Desktop/niu/.omx/transcripts/BV1zwdUBrEhV-60to80min.txt)

### Observed mechanism signals

1. `Draw-First Decoding` stays dominant
2. `Topic Compression` strengthened
3. `Projection / plane-view reduction` support

### Immediate extraction judgment

The first 80 minutes of `BV1zwdUBrEhV` reinforce:

- `Draw-First Decoding`
- `Topic Compression`
- `Projection Readout`

## 80-93 Minute Extraction Notes (`BV1zwdUBrEhV`)

Working source:

- [BV1zwdUBrEhV-80to93min.txt](c:/Users/21604/Desktop/niu/.omx/transcripts/BV1zwdUBrEhV-80to93min.txt)

### Observed mechanism signals

1. `Draw-First Decoding` remains dominant
2. `characteristic-point / transcendental-point probing` strengthened
3. `Math-Physics Bridge` support

### Episode-Level Judgment For `BV1zwdUBrEhV`

After full-episode extraction, this episode strongly reinforces:

- `Draw-First Decoding`
- `Function Archetype Matching`
- `Boundary As Route Finder`
- `Projection Readout`

And gives additional support to:

- `Math-Physics Bridge`

## First 20-Minute Extraction Notes (`BV1EwdUBrE65`)

Working source:

- [BV1EwdUBrE65-first20min.txt](c:/Users/21604/Desktop/niu/.omx/transcripts/BV1EwdUBrE65-first20min.txt)

### Observed mechanism signals

1. `Special-Value Probing` support
2. `Puzzle Assignment` support
3. `Most-Constrained-First` support in combinatorics framing

### Immediate extraction judgment

The first 20 minutes of `BV1EwdUBrE65` reinforce:

- `Special-Value Probing`
- `Puzzle Assignment`
- `Most-Constrained-First`

## 20-40 Minute Extraction Notes (`BV1EwdUBrE65`)

Working source:

- [BV1EwdUBrE65-20to40min.txt](c:/Users/21604/Desktop/niu/.omx/transcripts/BV1EwdUBrE65-20to40min.txt)

### Observed mechanism signals

1. `Count-Do-Not-Derive` strongly strengthened
2. `Most-Constrained-First` stays active in arrangement/probability shells
3. `Symmetry As Variable Killer` support through equal-case compression
4. `block first / internal permutation later` support

### Immediate extraction judgment

The `20-40` minute section of `BV1EwdUBrE65` is not teaching isolated counting tricks. It keeps forcing the same bottom logic:

- do not reach for a formula if the state space can be read by hand
- if some objects must stay together, package them into one temporary object first
- if the cases are symmetric, compress them into one representative branch instead of re-solving all branches
- if a restriction creates empty-slot structure, count the slots directly

So this segment mainly reinforces:

- `Count-Do-Not-Derive`
- `Most-Constrained-First`
- `Symmetry As Variable Killer`

## 40-60 Minute Extraction Notes (`BV1EwdUBrE65`)

Working source:

- [BV1EwdUBrE65-40to60min.txt](c:/Users/21604/Desktop/niu/.omx/transcripts/BV1EwdUBrE65-40to60min.txt)

### Observed mechanism signals

1. `Count-Do-Not-Derive` keeps dominating
2. `Symmetry As Variable Killer` is used to halve order-constrained permutation space
3. `Most-Constrained-First` appears again in forbidden-choice/counting shells
4. `route switching when one counting lens becomes messy` support

### Immediate extraction judgment

This `40-60` minute segment keeps pushing one very important bottom logic: the problem is often already solved once the state encoding is chosen correctly.

Repeated routes in this chunk:

- if two relative orders are symmetric, count one and take half
- if a path is made of fixed numbers of horizontal/vertical moves, forget the picture and count the move-word
- if the direct inclusion route gets messy, flip to the complementary or troublemaker-first route

So this segment mainly reinforces:

- `Count-Do-Not-Derive`
- `Symmetry As Variable Killer`
- `Most-Constrained-First`

## 60-80 Minute Extraction Notes (`BV1EwdUBrE65`)

Working source:

- [BV1EwdUBrE65-60to80min.txt](c:/Users/21604/Desktop/niu/.omx/transcripts/BV1EwdUBrE65-60to80min.txt)

### Observed mechanism signals

1. `Count-Do-Not-Derive` remains dominant
2. `Symmetry As Variable Killer` is repeatedly used to reduce repeated order/case work
3. `same/different pattern first, labels later` support
4. `fix the problematic part first, then free-fill the rest` support

### Immediate extraction judgment

This `60-80` minute segment keeps reinforcing a deeper combinatorics compression logic:

- first decide the structural pattern
- only after the pattern is fixed, restore labels, colors, or internal permutations
- when some objects are indistinguishable on the current layer, do not over-count them as if they were already labeled

So this segment mainly reinforces:

- `Count-Do-Not-Derive`
- `Symmetry As Variable Killer`
- pattern-first counting

## 80-95 Minute Extraction Notes (`BV1EwdUBrE65`)

Working source:

- [BV1EwdUBrE65-last15.txt](c:/Users/21604/Desktop/niu/.omx/transcripts/BV1EwdUBrE65-last15.txt)

### Observed mechanism signals

1. `Count-Do-Not-Derive` still dominates the local combinatorics shells
2. `sequence/probability recursion -> series model` support
3. `aggregate-ratio readout under symmetry/exchangeability` support
4. `split only where structure actually differs` support

### Immediate extraction judgment

This final segment adds an important extension to the season's counting logic:

- local probability/combinatorics questions are still solved by explicit state counting
- but once the process has memory, the route upgrades to “write the probability sequence, then normalize it into a standard recurrence/series”
- if the process is exchangeable enough, stop tracking individual draw order and read the answer from the global composition ratio directly

So this segment mainly reinforces:

- `Count-Do-Not-Derive`
- recurrence-as-sequence compression
- aggregate-ratio readout

### Episode-Level Judgment For `BV1EwdUBrE65`

After full-episode extraction, this episode strongly reinforces:

- `Count-Do-Not-Derive`
- `Most-Constrained-First`
- `Symmetry As Variable Killer`

And gives additional support to:

- pattern-first counting
- recurrence / sequence recasting for process probability
- aggregate-ratio readout under exchangeability

## First 20-Minute Extraction Notes (`BV1FcdUBVEfN`)

Working source:

- [BV1FcdUBVEfN-first20min.txt](c:/Users/21604/Desktop/niu/.omx/transcripts/BV1FcdUBVEfN-first20min.txt)

### Observed mechanism signals

1. `Special-Value Probing` support
2. `Count-Do-Not-Derive` strengthened on sequence questions
3. `Guess Then Verify` / `Rapid Verification` support in multiple-choice shells
4. `Canonical Normalization` support when conditions are insufficient

### Immediate extraction judgment

The first 20 minutes of `BV1FcdUBVEfN` are mechanism-heavy in a different way from the probability episode. The repeated bottom logic is:

- if a sequence condition is underdetermined, choose the most convenient normalized special case first
- do not rush to a closed form; write the first few terms and read the pattern
- in choice questions, test a cheap witness value early and kill wrong routes before full derivation

So this segment mainly reinforces:

- `Special-Value Probing`
- `Count-Do-Not-Derive`
- `Guess Then Verify`
- `Canonical Normalization`

## 20-40 Minute Extraction Notes (`BV1FcdUBVEfN`)

Working source:

- [BV1FcdUBVEfN-20to40min.txt](c:/Users/21604/Desktop/niu/.omx/transcripts/BV1FcdUBVEfN-20to40min.txt)

### Observed mechanism signals

1. `Count-Do-Not-Derive` stays dominant
2. `Guess Then Verify` / `Rapid Verification` keep appearing in choice questions
3. `Special-Value Probing` support through low-index term testing
4. `pattern readout before exact derivation` support

### Immediate extraction judgment

The `20-40` minute segment keeps forcing one bottom rule: if the exact route is ugly, force the sequence to reveal itself locally first.

Repeated routes in this chunk:

- compute `A1`, `A2`, `A3` first and look for the law
- plug `n=1` or another cheap index to eliminate options immediately
- when a sum is built from terms all close to `1`, estimate the scale first before exact handling

So this segment mainly reinforces:

- `Count-Do-Not-Derive`
- `Guess Then Verify`
- local-pattern readout

## 40-60 Minute Extraction Notes (`BV1FcdUBVEfN`)

Working source:

- [BV1FcdUBVEfN-40to60min.txt](c:/Users/21604/Desktop/niu/.omx/transcripts/BV1FcdUBVEfN-40to60min.txt)

### Observed mechanism signals

1. `Count-Do-Not-Derive` remains dominant
2. `period detection before formula derivation` support
3. `invert / index-weight transform to linearize recurrence` support
4. `Most-Constrained-First` support through “ask what object should become the new sequence”

### Immediate extraction judgment

This `40-60` minute segment is very strong on transformation logic rather than plain calculation.

Repeated bottom routes:

- if the sequence is messy, compute enough terms to see whether it is actually periodic
- if the recurrence contains awkward division or an `n` weight, choose a new sequence so the recurrence becomes linear
- do not keep pushing on the original object if another view of the same object is easier to govern

So this segment mainly reinforces:

- `Count-Do-Not-Derive`
- transform-to-linearize
- period detection

## 60-80 Minute Extraction Notes (`BV1FcdUBVEfN`)

Working source:

- [BV1FcdUBVEfN-60to80min.txt](c:/Users/21604/Desktop/niu/.omx/transcripts/BV1FcdUBVEfN-60to80min.txt)

### Observed mechanism signals

1. `Definition As Direct Readout` support for telescoping forms
2. `Puzzle Assignment` support through step-ladder question design
3. `Constructive Surrogate Building` support in `bn` substitution / difference-times-geometric recasting
4. `Count-Do-Not-Derive` continues through pattern-first derivation control

### Immediate extraction judgment

This `60-80` minute segment upgrades the sequence toolkit from local probing to reusable route templates.

Repeated bottom routes:

- if the denominator structure already screams telescoping, read it directly and split it
- if one subquestion looks strangely easy, treat it as the scaffold for the next subquestion
- when the target sequence looks like “arithmetic times geometric,” re-encode it into the standard difference-times-geometric shell instead of deriving from scratch

So this segment mainly reinforces:

- `Definition As Direct Readout`
- `Puzzle Assignment`
- `Constructive Surrogate Building`

## 80-92 Minute Extraction Notes (`BV1FcdUBVEfN`)

Working source:

- [BV1FcdUBVEfN-80to92min.txt](c:/Users/21604/Desktop/niu/.omx/transcripts/BV1FcdUBVEfN-80to92min.txt)

### Observed mechanism signals

1. `Definition As Direct Readout` stays strong on telescoping and mixed-sequence shells
2. `Constructive Surrogate Building` strengthened for arithmetic-geometric templates
3. `Rapid Verification` reinforced through mandatory answer back-checking
4. `Model Calling Before Derivation` support via “first classify the sequence type, then compute”

### Immediate extraction judgment

This final segment is less about new tricks and more about operational discipline:

- first classify what kind of object you are looking at
- once the shell is identified, route to the matching template directly
- even after the route is clear, back-check low-cost anchors so small algebra slips do not survive

So this segment mainly reinforces:

- `Definition As Direct Readout`
- `Constructive Surrogate Building`
- `Rapid Verification`

### Episode-Level Judgment For `BV1FcdUBVEfN`

After full-episode extraction, this episode strongly reinforces:

- `Count-Do-Not-Derive`
- `Canonical Normalization`
- `Constructive Surrogate Building`

And gives additional support to:

- `Guess Then Verify`
- `Rapid Verification`
- `Definition As Direct Readout`

The main drop-dimension axis of this episode is:

- do not hard-push the original sequence object
- first let small prefixes and cheap witnesses expose the structure
- then re-encode the sequence into a governable object
- then route it into a standard template such as periodic, telescoping, arithmetic, geometric, or arithmetic-geometric

## First 20-Minute Extraction Notes (`BV1BNdUBzE3E`)

Working source:

- [BV1BNdUBzE3E-first20min.txt](c:/Users/21604/Desktop/niu/.omx/transcripts/BV1BNdUBzE3E-first20min.txt)

### Observed mechanism signals

1. `Special-Value Probing` support
2. `Canonical Normalization` strengthened under insufficient conditions
3. `Model Calling Before Derivation` support through constant/periodic/simple-model substitution
4. `Rapid Verification` support through legal counterexample screening

### Immediate extraction judgment

The first 20 minutes of `BV1BNdUBzE3E` are not really about one chapter of sequences. The bottom logic is:

- if the condition set is insufficient, do not stay trapped in the original abstract object
- first build the easiest legal world
- then use that world to kill wrong options, expose invariants, or read the structure directly

This segment is therefore pushing a strong drop-dimension route:

- identify underdetermination
- normalize to a cheap model
- use witness-style verification before full proof

So this segment mainly reinforces:

- `Special-Value Probing`
- `Canonical Normalization`
- `Model Calling Before Derivation`
- `Rapid Verification`

## 20-40 Minute Extraction Notes (`BV1BNdUBzE3E`)

Working source:

- [BV1BNdUBzE3E-20to40min.txt](c:/Users/21604/Desktop/niu/.omx/transcripts/BV1BNdUBzE3E-20to40min.txt)

### Observed mechanism signals

1. `Special-Value Probing` strengthened
2. `Guess Then Verify` support through common-model guessing
3. `Model Calling Before Derivation` remains dominant
4. `Projection Readout` support in triangle/geometry items

### Immediate extraction judgment

The `20-40` minute section of `BV1BNdUBzE3E` keeps repeating a very practical bottom rule:

- when the family is familiar, guess the common shell first
- test the shell on a few points or indices
- if the shell survives, stop doing unnecessary symbolic work

This chunk also extends the same logic into geometry:

- do not start from formula expansion
- first collapse the figure into the standard picture you already know how to read

So this segment mainly reinforces:

- `Special-Value Probing`
- `Guess Then Verify`
- `Model Calling Before Derivation`

## 40-60 Minute Extraction Notes (`BV1BNdUBzE3E`)

Working source:

- [BV1BNdUBzE3E-40to60min.txt](c:/Users/21604/Desktop/niu/.omx/transcripts/BV1BNdUBzE3E-40to60min.txt)

### Observed mechanism signals

1. `Special-Value Probing` stays dominant
2. `Projection Readout` strengthened in function/geometry shells
3. `Canonical Normalization` support via ultra-simple witness substitution
4. `Answer-Shape Pressure` support through exam-first route choice

### Immediate extraction judgment

This `40-60` minute segment pushes a stronger exam-oriented bottom logic:

- difficult-looking function and geometry items are still first collapsed into a drawable or low-cost test world
- the point is not elegance, but to get the answer shape or truth value quickly without burning mental budget
- simple witnesses are used not only to falsify, but to expose the hidden graph or extremum picture

So this segment mainly reinforces:

- `Special-Value Probing`
- `Projection Readout`
- `Canonical Normalization`
- `Answer-Shape Pressure`

## 60-80 Minute Extraction Notes (`BV1BNdUBzE3E`)

Working source:

- [BV1BNdUBzE3E-60to80min.txt](c:/Users/21604/Desktop/niu/.omx/transcripts/BV1BNdUBzE3E-60to80min.txt)

### Observed mechanism signals

1. `Special-Value Probing` remains dominant
2. `Guess Then Verify` strengthened on vectors/functions
3. `Model Calling Before Derivation` support through constant/simple-model substitution
4. `Projection Readout` support in geometric and vector shells

### Immediate extraction judgment

This `60-80` minute segment continues the same deeper logic from earlier parts of the episode:

- if a problem allows a legal toy world, move into it immediately
- let that toy world expose answer direction before any full derivation
- in vectors/functions, the cheap picture or the cheap witness usually carries more signal than raw algebra

So this segment mainly reinforces:

- `Special-Value Probing`
- `Guess Then Verify`
- `Projection Readout`

## 20-40 Minute Extraction Notes (`BV17NdUBzE73`)

Working source:

- [BV17NdUBzE73-20to40min.txt](c:/Users/21604/Desktop/niu/.omx/transcripts/BV17NdUBzE73-20to40min.txt)

### Observed mechanism signals

1. `Projection Readout` stays dominant
2. `Model Calling Before Derivation` support on vectors and geometry
3. `Special-Value Probing` support through simplest legal vector configurations
4. `Answer-Shape Pressure` support by screening via obvious geometric impossibility

### Immediate extraction judgment

The `20-40` minute segment keeps pushing the same visual route:

- when vector conditions look underdetermined, freeze the easiest legal configuration
- once the picture is fixed, many options die by impossible length/position relations
- geometric plausibility becomes a first-pass answer filter before calculation

So this segment mainly reinforces:

- `Projection Readout`
- `Model Calling Before Derivation`
- `Special-Value Probing`

## 20-40 Minute Extraction Notes (`BV1EPdUBgE7y`)

Working source:

- [BV1EPdUBgE7y-20to40min.txt](c:/Users/21604/Desktop/niu/.omx/transcripts/BV1EPdUBgE7y-20to40min.txt)

### Observed mechanism signals

1. `Projection Readout` remains dominant
2. `Canonical Normalization` support via simplifying vector positions
3. `Special-Value Probing` strengthened
4. `Guess Then Verify` support in option elimination

### Immediate extraction judgment

This `20-40` minute segment keeps reinforcing a sharp vector bottom logic:

- if the degrees of freedom are too many, immediately pin down the cheapest legal configuration
- then use that configuration as a witness world to eliminate wrong options
- visual and positional constraints act as a stronger first filter than symbolic expansion

So this segment mainly reinforces:

- `Projection Readout`
- `Canonical Normalization`
- `Guess Then Verify`

## First 20-Minute Extraction Notes (`BV17NdUBzE73`)

Working source:

- [BV17NdUBzE73-first20min.txt](c:/Users/21604/Desktop/niu/.omx/transcripts/BV17NdUBzE73-first20min.txt)

### Observed mechanism signals

1. `Projection Readout` strongly strengthened
2. `Symmetry As Variable Killer` support
3. `Definition As Direct Readout` support for mean-value inequalities
4. `Special-Value Probing` support through low-cost function/triangle witnesses

### Immediate extraction judgment

The first 20 minutes of `BV17NdUBzE73` are very visual. The bottom logic is:

- do not treat inequalities and vectors as symbol stacks
- first convert them into pictures with center, balance, angle, or shape
- once the picture is visible, the decisive relation is often directly readable

So this segment mainly reinforces:

- `Projection Readout`
- `Symmetry As Variable Killer`
- `Definition As Direct Readout`

## First 20-Minute Extraction Notes (`BV1EPdUBgE7y`)

Working source:

- [BV1EPdUBgE7y-first20min.txt](c:/Users/21604/Desktop/niu/.omx/transcripts/BV1EPdUBgE7y-first20min.txt)

### Observed mechanism signals

1. `Projection Readout` strongly strengthened
2. `Model Calling Before Derivation` support in vector problems
3. `Canonical Normalization` support through coordinate/special-position fixing
4. `Special-Value Probing` support in abstract vector shells

### Immediate extraction judgment

The first 20 minutes of `BV1EPdUBgE7y` are pushing one very clear bottom logic:

- vectors are not for coordinate grind first
- they are first to be turned into pictures, standard positions, or the simplest legal configuration
- once the picture is fixed, many answer relations become immediate

So this segment mainly reinforces:

- `Projection Readout`
- `Model Calling Before Derivation`
- `Canonical Normalization`

## First 20-Minute Extraction Notes (`BV1JwdUBrETb`)

Working source:

- [BV1JwdUBrETb-first20min.txt](c:/Users/21604/Desktop/niu/.omx/transcripts/BV1JwdUBrETb-first20min.txt)

### Observed mechanism signals

1. `Count-Do-Not-Derive` strengthened
2. `Most-Constrained-First` support
3. `process/schedule style probability routing` support

### Immediate extraction judgment

The first 20 minutes of `BV1JwdUBrETb` reinforce:

- `Count-Do-Not-Derive`
- `Most-Constrained-First`
- process/schedule style probability routing

## 20-40 Minute Extraction Notes (`BV1JwdUBrETb`)

Working source:

- [BV1JwdUBrETb-20to40min.txt](c:/Users/21604/Desktop/niu/.omx/transcripts/BV1JwdUBrETb-20to40min.txt)

### Observed mechanism signals

1. `Completion Methods` strengthened
2. `Model Calling Before Derivation` strengthened
3. supplemented-solid routing dominates

### Immediate extraction judgment

The first 40 minutes of `BV1JwdUBrETb` reinforce:

- `Completion Methods`
- `Model Calling Before Derivation`
- supplemented-solid routing

## 40-60 Minute Extraction Notes (`BV1JwdUBrETb`)

Working source:

- [BV1JwdUBrETb-40to60min.txt](c:/Users/21604/Desktop/niu/.omx/transcripts/BV1JwdUBrETb-40to60min.txt)

### Observed mechanism signals

1. `Most-Constrained-First` support
2. `helper-function monotonicity` support
3. `discuss-valid-cases-before-solving` support

### Immediate extraction judgment

The first 60 minutes of `BV1JwdUBrETb` reinforce:

- `Most-Constrained-First`
- helper-function monotonicity routing
- structured validity discussion

## 60-80 Minute Extraction Notes (`BV1JwdUBrETb`)

Working source:

- [BV1JwdUBrETb-60to80min.txt](c:/Users/21604/Desktop/niu/.omx/transcripts/BV1JwdUBrETb-60to80min.txt)

### Observed mechanism signals

1. `Answer-Shape Pressure` support
2. `Completion Methods` stays dominant
3. `screen candidates before exact sphere arithmetic` support

### Immediate extraction judgment

The first 80 minutes of `BV1JwdUBrETb` reinforce:

- `Completion Methods`
- `Answer-Shape Pressure`
- screen-first solid-geometry routing

## 80-94 Minute Extraction Notes (`BV1JwdUBrETb`)

Working source:

- [BV1JwdUBrETb-80to94min.txt](c:/Users/21604/Desktop/niu/.omx/transcripts/BV1JwdUBrETb-80to94min.txt)

### Observed mechanism signals

1. `Projection Readout` strengthened
2. `distance-constant -> sphere/section` support
3. `helper-vector selection` support

### Episode-Level Judgment For `BV1JwdUBrETb`

After full-episode extraction, this episode strongly reinforces:

- `Count-Do-Not-Derive`
- `Most-Constrained-First`
- `Completion Methods`
- `Projection Readout`

And gives additional support to:

- `Answer-Shape Pressure`

## 20-40 Minute Extraction Notes (`BV1JwdUBrETb`)

Working source:

- [BV1JwdUBrETb-20to40min.txt](c:/Users/21604/Desktop/niu/.omx/transcripts/BV1JwdUBrETb-20to40min.txt)

### Observed mechanism signals

1. `Completion Methods` strengthened
2. `Model Calling Before Derivation` strengthened
3. `solid-geometry supplement / complete-the-whole` support

### Immediate extraction judgment

The first 40 minutes of `BV1JwdUBrETb` reinforce:

- `Completion Methods`
- `Model Calling Before Derivation`
- supplemented-solid routing

## 40-60 Minute Extraction Notes (`BV1JwdUBrETb`)

Working source:

- [BV1JwdUBrETb-40to60min.txt](c:/Users/21604/Desktop/niu/.omx/transcripts/BV1JwdUBrETb-40to60min.txt)

### Observed mechanism signals

1. `Most-Constrained-First` support in root-validity discussion
2. `Completion Methods` remains dominant
3. `derive/build helper function and prove monotonicity` support

### Immediate extraction judgment

The first 60 minutes of `BV1JwdUBrETb` reinforce:

- `Most-Constrained-First`
- `Completion Methods`
- helper-function monotonicity routing

## 60-80 Minute Extraction Notes (`BV1JwdUBrETb`)

Working source:

- [BV1JwdUBrETb-60to80min.txt](c:/Users/21604/Desktop/niu/.omx/transcripts/BV1JwdUBrETb-60to80min.txt)

### Observed mechanism signals

1. `Answer-Shape Pressure` support
2. `Completion Methods` stays dominant on solid-geometry items
3. `do not fully compute when candidate form already rules out options`

### Immediate extraction judgment

The first 80 minutes of `BV1JwdUBrETb` reinforce:

- `Completion Methods`
- `Answer-Shape Pressure`
- estimate/screen-first routing

## 40-60 Minute Extraction Notes (`BV1JwdUBrETb`)

Working source:

- [BV1JwdUBrETb-40to60min.txt](c:/Users/21604/Desktop/niu/.omx/transcripts/BV1JwdUBrETb-40to60min.txt)

### Observed mechanism signals

1. `Completion Methods` strengthened
2. `Most-Constrained-First` support for root/sign discussions
3. `build a helper function and prove monotonicity` support

### Immediate extraction judgment

The first 60 minutes of `BV1JwdUBrETb` reinforce:

- `Completion Methods`
- `Most-Constrained-First`
- monotonic helper-function routing

## 20-40 Minute Extraction Notes (`BV1JwdUBrETb`)

Working source:

- [BV1JwdUBrETb-20to40min.txt](c:/Users/21604/Desktop/niu/.omx/transcripts/BV1JwdUBrETb-20to40min.txt)

### Observed mechanism signals

1. `Completion / supplementation model` support
2. `Model Calling Before Derivation` strengthened
3. `Completion Methods` / `flagpole-style and supplement-style solid geometry` support

### Immediate extraction judgment

The first 40 minutes of `BV1JwdUBrETb` reinforce:

- solid-geometry completion models
- `Model Calling Before Derivation`
- `Completion Methods`

## 40-60 Minute Extraction Notes (`BV1EPdUBgE7y`)

Working source:

- transcript lines 500-900

### Observed mechanism signals

1. `Projection Readout` remains dominant
   - the speaker solves all vector dot-product and optimization problems by drawing the projection picture first
   - tangent-line conditions for extrema are handled visually: "相切时候最大"

2. `Draw-First Decoding` strengthened
   - the speaker explicitly says every vector problem should be drawn with AB/AC as coordinate axes because "垂直看起来好做"
   - option elimination is done purely from the drawn figure: sign, magnitude, relative size

3. `Special-Value Probing` support
   - when the problem has too many degrees of freedom, the speaker immediately uses an isosceles right triangle or equilateral triangle as the test case
   - "答案和P点任何位置没关系, 我就让P点就变成这样子就好了"

4. `Boundary As Route Finder` support
   - vector tangent/extremum problems are routed by finding the tangent configuration on a circle in the drawn picture

### Immediate extraction judgment

The `40-60` minute segment keeps reinforcing a very sharp vector bottom logic:

- draw vectors as coordinate axes first
- read sign and magnitude from the picture to eliminate options
- for optimization, find the tangent configuration visually
- for dot products, read the projection directly from the drawing

So this segment mainly reinforces:

- `Projection Readout`
- `Draw-First Decoding`
- `Special-Value Probing`

## 60-80 Minute Extraction Notes (`BV1EPdUBgE7y`)

Working source:

- transcript lines 900-1200

### Observed mechanism signals

1. `Definition As Direct Readout` support
   - the polarization identity is presented as a direct algebraic expansion tool: AC·AD = ¼[(AC+AD)² − (AC−AD)²]
   - the speaker says it works but "不如画图来得快"
   - equal-sum lines (等和线) are explained as a direct geometric concept: all points with λ+μ = constant lie on a parallel line

2. `Model Calling Before Derivation` support
   - the four triangle centers (circumcenter, centroid, orthocenter, incenter) are each reduced to their vector definition
   - centroid: OA+OB+OC = 0
   - circumcenter: equal distances
   - orthocenter: equal projections
   - incenter: weighted by side lengths
   - the speaker says "知道定义就好"

3. `Projection Readout` continues
   - orthocenter is explained as "投影都是这段"
   - the speaker keeps collapsing vector relations to projection pictures

### Immediate extraction judgment

The `60-80` minute segment transitions from pure vector problems to geometric center definitions and exam-specific tools:

- polarization identity works but visual methods are faster for exam purposes
- equal-sum lines provide a direct readout for coefficient-constrained vector problems
- four centers are each reduced to one defining vector relation

So this segment mainly reinforces:

- `Definition As Direct Readout`
- `Model Calling Before Derivation`
- `Projection Readout`

## 80-98 Minute Extraction Notes (`BV1EPdUBgE7y`)

Working source:

- transcript lines 1200-1497

### Observed mechanism signals

1. `Count-Do-Not-Derive` strongly strengthened
   - the speaker solves a five-person arrangement problem with adjacency constraints by exhaustive position counting
   - explicitly: "高考的题纸都是数的出来的, 都给我数, 数穷尽了就完了"
   - two separate arrangement examples are solved by drawing all five positions and counting directly

2. `Most-Constrained-First` support
   - in the arrangement problem, 甲 (restricted from endpoints) is placed first, then 乙丙 (must be separated by ≥2), then the rest fill freely
   - "会哭的孩子先哄睡"

3. `Draw-First Decoding` support
   - the speaker draws line intercepts for a straight-line problem and reads the trajectory as a parallel line directly from the picture
   - solid geometry is handled by drawing the two perpendicular planes and checking parallel/perpendicular relations visually

4. `Function Archetype Matching` strengthened
   - a trigonometric problem is solved by tan substitution, leading to tan θ = -1/2 or tan θ = 2
   - the speaker immediately recognizes this as the 1-2-√5 triangle (26.5°)
   - doubling the angle gives 53° → recognized as the 3-4-5 triangle
   - "这是26.5度, 两倍角就是53度, 这是三四五"

5. `Special-Value Probing` support
   - the speaker uses the golden triangle inside an ellipse (c:b = 1:2, giving 1-√3-2 triangle) to read the eccentricity directly from the picture

### Immediate extraction judgment

The `80-98` minute segment is the nine-province joint exam walkthrough. The bottom logic is:

- statistics: sort and count directly instead of memorizing formulas
- conics: draw the golden triangle and read ratios
- arrangements: place the most constrained first, then count exhaustively
- solid geometry: draw the configuration and check relations visually
- trigonometry: tan substitution → recognize special triangle archetype

So this segment mainly reinforces:

- `Count-Do-Not-Derive`
- `Most-Constrained-First`
- `Function Archetype Matching`
- `Draw-First Decoding`

### Episode-Level Judgment For `BV1EPdUBgE7y`

After full-episode extraction, this episode strongly reinforces:

- `Projection Readout`
- `Draw-First Decoding`
- `Canonical Normalization`
- `Special-Value Probing`

And gives additional support to:

- `Count-Do-Not-Derive`
- `Most-Constrained-First`
- `Function Archetype Matching`
- `Definition As Direct Readout`
- `Model Calling Before Derivation`

The main drop-dimension axis of this episode is:

- vectors are pictures first, coordinates second
- all vector problems start with drawing AB/AC as orthogonal axes
- all exam problems are attacked by drawing, counting, or recognizing archetypes before any symbolic manipulation

## First 20-Minute Extraction Notes (`BV17PdUBgEZA`)

Working source:

- transcript lines 1-500

### Observed mechanism signals

1. `Function Archetype Matching` strongly strengthened
   - the speaker introduces seven special triangles as a reusable catalogue:
     - equilateral 1-1-1 (60°)
     - 30-60-90: 1-√3-2
     - isosceles right: 1-1-√2 (45°)
     - 3-4-5 (37°-53°)
     - 1-2-√5 (26.5°) — half-angle of 3-4-5
     - 1-3-√10 (18.5°) — half-angle of 37°
     - 7-24-25 (74°) — double of 37°
   - these seven triangles are taught as recognizable archetypes that cover 95%+ of exam triangle problems

2. `Model Calling Before Derivation` strongly strengthened
   - the speaker introduces three main triangle problem models:
     - **Apollonius circle (R圆/PC圆三角形)**: fixed base, constant ratio of sides → vertex moves on a circle
     - **Circumscribed-angle triangle (圆直角三角形)**: fixed base, fixed opposite angle → vertex moves on a circular arc
     - **Ellipse triangle (椭圆三角形)**: fixed base, constant sum of other two sides → vertex moves on an ellipse
   - the speaker claims these three models cover 98%+ of Gaokao triangle problems

3. `Boundary As Route Finder` support
   - each model has its extremum at a specific geometric configuration:
     - Apollonius circle: area max when vertex is at top of circle (面积 = ½ × 底 × 半径)
     - Circumscribed-angle: symmetric position gives maximum area
     - Ellipse: symmetric position gives maximum area

4. `Answer-Shape Pressure` support
   - the speaker explicitly says exam designers choose "nice" numbers that correspond to the seven special triangles
   - recognizing the triangle archetype immediately narrows the answer space

### Immediate extraction judgment

The first 20 minutes of `BV17PdUBgEZA` are extremely model-heavy. The bottom logic is:

- triangle problems are not solved by raw sine/cosine law application
- they are first classified into one of three geometric models
- once classified, the extremum/answer is read from the model's geometric properties
- the seven special triangles serve as the recognition vocabulary for quick identification

So this segment mainly reinforces:

- `Function Archetype Matching`
- `Model Calling Before Derivation`
- `Boundary As Route Finder`

## 20-40 Minute Extraction Notes (`BV17PdUBgEZA`)

Working source:

- transcript lines 500-1000

### Observed mechanism signals

1. `Guess Then Verify` strongly strengthened
   - the speaker explicitly teaches that 80% of Gaokao triangle angle answers are 60° (π/3)
   - the strategy is: guess 60° first, then construct the formal proof
   - "高考答题过程不是解题过程, 是表演过程"
   - "先猜后证, 这是最省心的方法"

2. `Special-Value Probing` strengthened
   - equilateral triangle is the default test case for all triangle optimization problems
   - "等边三角形时面积最大" is used as the default guess

3. `Function Archetype Matching` continues
   - the speaker repeatedly identifies specific angle values (26.5°, 18.5°, 37°, 53°, 74°) as triggers for recognizing the seven special triangles
   - "看到这个角度, 你就知道是哪个三角形"

4. `Answer-Shape Pressure` strengthened
   - the speaker explicitly says exam problems are designed with "nice" answers
   - "高考不会出不好算的数, 答案一定是好看的"

### Immediate extraction judgment

The `20-40` minute segment is the philosophical core of the triangle approach:

- guess the answer first (usually 60°), then construct a formal proof
- treat the exam answer as a performance, not a discovery process
- use equilateral triangle as the default test configuration
- recognize angle values as instant triggers for the seven-triangle catalogue

So this segment mainly reinforces:

- `Guess Then Verify`
- `Special-Value Probing`
- `Answer-Shape Pressure`

## 40-60 Minute Extraction Notes (`BV17PdUBgEZA`)

Working source:

- transcript lines 1000-1498

### Observed mechanism signals

1. `Projection Readout` strongly strengthened
   - the projection theorem (射影定理/摄影定理) is taught as a fundamental tool:
     - a = b·cosC + c·cosB (each side equals the sum of projections of the other two sides)
   - also called "三弦定律" or "高阶定律"
   - the speaker frames this as more powerful than raw sine/cosine law for many problems

2. `Definition As Direct Readout` strengthened
   - the median theorem is taught: 2(m² + (a/2)²) = b² + c²
   - the angle bisector theorem is taught: bisector divides opposite side in ratio b:c
   - bisector length is derived via equal-area method
   - each theorem provides a direct readout rather than requiring derivation from scratch

3. `Rapid Verification` strongly strengthened
   - the speaker repeatedly warns: "第一问必须验算, 错了第二问全废"
   - first sub-question results must be back-checked because cascading errors are catastrophic

4. `Model Calling Before Derivation` continues
   - all three triangle models are applied to concrete problems
   - the speaker routes problems by first asking: "这是R圆, 圆周角, 还是椭圆?"

### Immediate extraction judgment

The `40-60` minute segment is tool-heavy:

- three line theorems (projection, median, angle bisector) are taught as compact readout tools
- the projection theorem is positioned as especially powerful for direct calculations
- mandatory verification of first sub-question is stressed as exam discipline
- every problem is first classified by model type before any calculation begins

So this segment mainly reinforces:

- `Projection Readout`
- `Definition As Direct Readout`
- `Rapid Verification`
- `Model Calling Before Derivation`

## 60-80 Minute Extraction Notes (`BV17PdUBgEZA`)

Working source:

- transcript lines 1498-1997

### Observed mechanism signals

1. `Guess Then Verify` remains dominant
   - the speaker continues to demonstrate the guess-then-verify approach on concrete problems
   - angle guessing (60°), side-ratio guessing (equilateral), then formal verification

2. `Special-Value Probing` strengthened
   - the speaker uses equilateral triangle as the universal first probe for optimization problems
   - basic inequality (AM-GM) is applied after guessing the equality condition

3. `Projection Readout` support
   - projection theorem is applied to concrete problems for direct computation
   - the speaker demonstrates reading median length and angle bisector length from the theorem formulas directly

4. `Model Calling Before Derivation` continues
   - the three triangle models keep being applied: identify the model, then compute within its framework

### Immediate extraction judgment

The `60-80` minute segment is a problem-solving demonstration block:

- every problem starts with model identification
- then the answer is guessed (usually 60° or equilateral)
- then verified using the three-line theorems or basic inequality
- the speaker keeps emphasizing this saves time and builds confidence

So this segment mainly reinforces:

- `Guess Then Verify`
- `Special-Value Probing`
- `Projection Readout`

## 80-96 Minute Extraction Notes (`BV17PdUBgEZA`)

Working source:

- transcript lines 1997-2231

### Observed mechanism signals

1. `Rapid Verification` strengthened
   - the speaker reiterates that first-question answers must be verified before proceeding
   - "第一问写错了, 第二问全部白做"

2. `Guess Then Verify` continues
   - the speaker demonstrates guessing median-related range values and then verifying with the median theorem

3. `Model Calling Before Derivation` strengthened
   - the three triangle models are explicitly summarized as covering the vast majority of Gaokao triangle problems
   - the speaker says all these methods aim to make problem-solving "更快, 更准, 更省心"

4. `Answer-Shape Pressure` support
   - the speaker stresses that all three models and seven triangles exist to save time for harder problems later in the exam

### Episode-Level Judgment For `BV17PdUBgEZA`

After full-episode extraction, this episode strongly reinforces:

- `Function Archetype Matching` (seven special triangles as recognition catalogue)
- `Model Calling Before Derivation` (three triangle model types)
- `Guess Then Verify` (80% of angles are 60°, guess first then prove)
- `Projection Readout` (projection theorem as core tool)

And gives additional support to:

- `Special-Value Probing`
- `Rapid Verification`
- `Definition As Direct Readout`
- `Answer-Shape Pressure`
- `Boundary As Route Finder`

The main drop-dimension axis of this episode is:

- triangle problems are first classified into three model types
- the seven special triangles serve as the vocabulary for instant recognition
- answers are guessed first (usually 60° or equilateral) then verified
- three-line theorems (projection, median, angle bisector) provide direct readout tools
- first sub-question must always be verified to prevent cascading errors

## First 20-Minute Extraction Notes (`BV1eVdUBPE5j`)

Working source:

- transcript lines 1-500

### Observed mechanism signals

1. `Function Archetype Matching` strongly strengthened
   - the speaker continues to teach the seven special triangles as the primary recognition system
   - concrete examples: seeing √2 → guess 1-1-√2; seeing √3 → guess 1-√3-2; seeing 3:4 ratio → guess 3-4-5
   - "看到这些数字, 你就应该立刻想到是哪个三角形"

2. `Guess Then Verify` strongly strengthened
   - the speaker introduces the five-character exam motto: **会、快、准、省、势**
     - 会 = can do it (school teaches this)
     - 快 = fast
     - 准 = accurate
     - 省 = save mental energy
     - 势 = build momentum/confidence
   - "学校只教会, 远远不够"
   - the exam strategy is to use body (drawing) to replace brain (calculation): "用体力代替脑力"

3. `Answer-Shape Pressure` strengthened
   - the speaker explicitly states: "巧合" (coincidence) is a design feature of exams
   - exam answers are deliberately designed to be "nice"
   - recognizing this allows test-takers to predict answers before full computation

4. `Special-Value Probing` support
   - equal-difference angle sequences in triangles → 80% the middle angle is 60° because A+B+C=180° and equal difference forces B=60°
   - this is used as both a probe and a primary solving route

### Immediate extraction judgment

The first 20 minutes of `BV1eVdUBPE5j` are meta-strategic:

- the five-character motto frames problem-solving as resource management, not just skill
- drawing and archetype recognition are the primary tools for speed, accuracy, and energy conservation
- exam design patterns ("coincidence") are treated as a valid source of information for route selection

So this segment mainly reinforces:

- `Function Archetype Matching`
- `Guess Then Verify`
- `Answer-Shape Pressure`

## 20-40 Minute Extraction Notes (`BV1eVdUBPE5j`)

Working source:

- transcript lines 500-1000

### Observed mechanism signals

1. `Function Archetype Matching` remains dominant
   - the speaker demonstrates recognizing special triangles in concrete Gaokao problems
   - sin B = cos B → B = 45° → isosceles right triangle → circumscribed-angle model, symmetric position gives maximum area

2. `Guess Then Verify` continues
   - the speaker guesses equilateral triangle for optimization, then verifies using AM-GM or direct computation
   - "先猜等边, 再验证"

3. `Model Calling Before Derivation` strengthened
   - the circumscribed-angle triangle model is applied: fixed base + fixed opposite angle → vertex on circular arc → symmetric position for area maximum

4. `Projection Readout` support
   - the speaker reads sin75° = (√6+√2)/4 and sin15° = (√6−√2)/4 as memorized values
   - these are presented as tools for direct readout rather than derivation

### Immediate extraction judgment

The `20-40` minute segment is a problem walkthrough block:

- each problem is first classified by triangle model type
- then the answer is guessed via archetype recognition
- then verified with minimal computation
- special values (sin75°, sin15°) are treated as memorized readout tools

So this segment mainly reinforces:

- `Function Archetype Matching`
- `Guess Then Verify`
- `Model Calling Before Derivation`

## 40-60 Minute Extraction Notes (`BV1eVdUBPE5j`)

Working source:

- transcript lines 1000-1498

### Observed mechanism signals

1. `Function Archetype Matching` stays dominant
   - the speaker demonstrates recognizing 1-2-√5 and 1-3-√10 triangles from specific angle values in real problems
   - 5-12-13 right triangle is mentioned as an additional useful archetype

2. `Guess Then Verify` continues
   - the speaker keeps demonstrating: guess the answer pattern (usually 60° or a specific special triangle), then construct the formal solution backward

3. `Draw-First Decoding` support
   - all problems are solved by drawing the triangle configuration first
   - "画图以后答案自动就出来了"

4. `Special-Value Probing` strengthened
   - the speaker uses the equilateral triangle as the universal probe for area optimization and range-finding problems

### Immediate extraction judgment

The `40-60` minute segment reinforces the same core loop:

- draw the triangle
- recognize the archetype from angle values or side ratios
- guess the answer
- verify with minimal computation

So this segment mainly reinforces:

- `Function Archetype Matching`
- `Guess Then Verify`
- `Draw-First Decoding`

## 60-80 Minute Extraction Notes (`BV1eVdUBPE5j`)

Working source:

- transcript lines 1498-1997

### Observed mechanism signals

1. `Guess Then Verify` remains dominant
   - the speaker keeps applying the guess-first strategy to increasingly complex problems
   - "中国考试给分不看方法, 答案对就给分"

2. `Function Archetype Matching` support
   - the speaker demonstrates vector problems solved by recognizing the underlying triangle as one of the seven special types
   - visual decomposition of vectors reveals the hidden triangle structure

3. `Projection Readout` support
   - vector problems are solved by visual projection/decomposition
   - "向量题都是画图, 然后投影分解"

4. `Special-Value Probing` strengthened
   - the speaker assumes angles are 60° as a default for multiple-choice questions, then works backward

### Immediate extraction judgment

The `60-80` minute segment extends the triangle toolkit to vectors and coordinate problems:

- vector problems are reinterpreted as triangle recognition problems
- the same seven archetypes and three models apply
- the guess-verify loop remains the dominant strategy

So this segment mainly reinforces:

- `Guess Then Verify`
- `Function Archetype Matching`
- `Projection Readout`

## 80-91 Minute Extraction Notes (`BV1eVdUBPE5j`)

Working source:

- transcript lines 1997-2287

### Observed mechanism signals

1. `Function Archetype Matching` strengthened
   - the speaker demonstrates recognizing 3-4-5 triangle in a conic/vector problem and reading the answer directly
   - "去年高考用七个三角形可以直接得50-86分"

2. `Boundary As Route Finder` support
   - the Monge circle (蒙日圆) is introduced: when two tangent lines to an ellipse are perpendicular, the intersection traces a circle
   - the speaker uses a special case (axes as tangent lines) to directly read the eccentricity

3. `Special-Value Probing` support
   - the Monge circle eccentricity is derived by plugging the simplest configuration (tangent lines along axes)

4. `Answer-Shape Pressure` support
   - the speaker concludes: "我讲的不是题, 是方法、是思维、是信心"
   - the overarching message: strategic thinking and pattern recognition beat brute-force calculation

### Episode-Level Judgment For `BV1eVdUBPE5j`

After full-episode extraction, this episode strongly reinforces:

- `Function Archetype Matching` (seven special triangles applied to real Gaokao problems)
- `Guess Then Verify` (five-character motto: 会快准省势)
- `Answer-Shape Pressure` (exam "coincidence" as design feature)
- `Draw-First Decoding` (all problems start with drawing)

And gives additional support to:

- `Special-Value Probing`
- `Model Calling Before Derivation`
- `Projection Readout`
- `Boundary As Route Finder` (Monge circle)

The main drop-dimension axis of this episode is:

- the seven special triangles are the recognition vocabulary for instant solving
- the five-character exam motto frames problem-solving as resource management
- exam design patterns are treated as valid information for route compression
- drawing + archetype recognition + guess-then-verify is the universal exam strategy

---

## Episode: `BV1VVdUBPEoZ` (ep016, 110:01)

This is a demonstration class focused on conic sections (圆锥曲线). The speaker walks through T8联考 and 2023 Gaokao problems using drawing, special triangles, elimination, and formula templates. The episode is structured as: conic basics → small-problem strategies → seven triangles → big-problem templates → exam philosophy.

### Segment: First 20 Minutes

Content: Introduction, conic section physical meaning (focal point = 聚焦的点, directrix = 瞄准的线, eccentricity = 偏离程度), first problem (T8联考 Q5: parabola with FA+FB+FC=0).

Mechanism signals:

1. `Canonical Normalization` — **strong, direct**
   - "三个点有三个未知数而题目只给了一个方程就意味着方程不够我就可以指定"
   - underdetermined system (3 points, 1 equation) → choose simplest legal configuration: A at vertex, BC symmetric
   - this is the same "build the cheapest legal world" move seen in earlier episodes

2. `Draw-First Decoding` — **strong, direct**
   - insistence on drawing parabola + directrix before any calculation
   - "一定要把准线画出来不要偷懒"

3. `Definition As Direct Readout` — **moderate**
   - physical meaning of focal point, directrix, eccentricity taught as direct geometric readout tools
   - "焦点就是聚焦的点, 准线就是瞄准的线"

### Segment: 20-40 Minutes

Content: 2023 exam problems — ellipse area-ratio (新二卷Q5), circle tangent angle (新卷Q6), hardest problem (乙卷Q12 PA·PD).

Mechanism signals:

1. `Answer-Shape Pressure` — **strong, direct, repeated**
   - ellipse area-ratio: "截距必须是负的所以正的就全错了" → sign constraint eliminates options
   - "必须要带根号不带根号的就错了" → radical-type constraint eliminates options
   - circle tangent: "不是90度比90度大所以一是不对的" → magnitude estimate eliminates options
   - "有理数减个无理数不可能变成整数" → number-type constraint eliminates options

2. `Projection Readout` — **strong, direct**
   - 乙卷Q12: "PA乘PD就是PQ·PD乘cos就这个投影PH" → reinterprets product as projection length
   - draws the figure and reads the projection directly

3. `Draw-First Decoding` — **strong, pervasive**
   - every problem starts with drawing; the speaker says "70%以上都是画完图答案自动出来了"

### Segment: 40-60 Minutes

Content: Seven special triangles catalogue, 全国一卷Q16 (hardest fill-in-blank), 海南理Q12.

Mechanism signals:

1. `Function Archetype Matching` — **strong, direct, systematic**
   - explicit seven-triangle catalogue:
     1. equilateral 1-1-1 (60°)
     2. half-equilateral 1-√3-2 (30°/60°)
     3. isosceles right 1-1-√2 (45°)
     4. 3-4-5 (37°/53°)
     5. 1-2-√5 (26.5°)
     6. 1-3-√10 (18.5°)
     7. 7-24-25 (74°)
   - "高考只考七个三角形这七个三角形可以精一的全国卷能得50~86分"
   - all seven are interconnected: 53°÷2=26.5° → 1-2-√5; 37°÷2=18.5° → 1-3-√10; 37°×2=74° → 7-24-25

2. `Guess Then Verify` — **strong, direct**
   - Q16: "我见三和二了我就猜它是不是三十五三角形" → sees 3 and 2, guesses 3-4-5, confirms
   - this is archetype-driven guessing: the triangle catalogue serves as the prior

3. `Guess Then Verify` (extreme-case method) — **strong, direct**
   - 乙卷大题: "我直接让它平过来PQ重合到这一点" → collapses the configuration to find the fixed point
   - "极限法立刻把答案猜出来"

### Segment: 60-80 Minutes

Content: More conic problems, function comparison (ex vs x vs lnx), sequence by finger-counting.

Mechanism signals:

1. `Count-Do-Not-Derive` — **strong, direct**
   - sequence problem: "数指头" method — literally counts paths (1→2→3→5) instead of computing to n=2023
   - "数列五个方法第二个方法就是手指头根本不用做所有题直接数出来"

2. `Draw-First Decoding` — **strong, direct**
   - function comparison: draws ex, x, lnx curves side by side to determine relative magnitudes of A, B, C
   - "画完图答案自动出来了"

3. `Projection Readout` — **moderate**
   - "交叉准线法" — uses directrix properties and cross-ratio to locate fixed lines on hyperbola

### Segment: 80-110 Minutes

Content: Solid geometry (regular tetrahedron), conic area maximum by symmetry, eight-formula template, exam philosophy.

Mechanism signals:

1. `Model Calling Before Derivation` — **strong, direct**
   - "加顶" — completes truncated solid to regular tetrahedron before solving
   - "系到轮台之后给我画出棱锥出来" — always reconstruct the full model first

2. `Symmetry As Variable Killer` — **strong, direct, repeated**
   - "对称出极值" — symmetry determines the extremum position for tangent-line area problems
   - "对称有两个点一个这个一个这个上面这个是小下面这个大"

3. `Definition As Direct Readout` — **strong, direct**
   - eight-formula template (八公式) for conic sections: stored formulas replace all derivation
   - "第八个公式切线公式把Y方变成Y0Y2X换成X加X0"
   - five-step method (五步法): set line → set endpoints → write condition → plug formulas → fake-derive → answer

4. `Answer-Shape Pressure` — **moderate**
   - "高考题能不算尽量不算时间真的很宝贵脑力也很宝贵" — resource-aware route selection
   - "小题不能大做" — first iron law of exam strategy

### Episode-Level Judgment For `BV1VVdUBPEoZ`

After full-episode extraction, this episode strongly reinforces:

- `Function Archetype Matching` (seven special triangles as systematic catalogue with interconnections)
- `Draw-First Decoding` (every problem solved by drawing first; "80%的画图")
- `Answer-Shape Pressure` (sign, radical-type, magnitude constraints for option elimination)
- `Guess Then Verify` (archetype-driven guessing + extreme-case method + special-case method)

And gives additional support to:

- `Canonical Normalization` (underdetermined systems → cheapest legal world)
- `Symmetry As Variable Killer` (symmetry determines extremum)
- `Model Calling Before Derivation` (complete the solid + eight-formula template)
- `Count-Do-Not-Derive` (finger-counting for sequences)
- `Definition As Direct Readout` (physical meaning of conic elements + formula templates)
- `Projection Readout` (product as projection + cross-ratio for fixed lines)

The main drop-dimension axis of this episode is:

- conic section problems are solved by drawing + seven-triangle recognition + elimination, not by algebra
- the eight-formula template turns conic big problems into parameter-filling exercises
- the five methods for guessing answers (特例法, 对称法, 极限法, 交叉准线法, 画图量法) form a complete toolkit
- exam problem-solving is framed as performance (表演), not derivation

---

## Episode: `BV15VdUBPETA` (ep022, 95:03)

This episode is devoted to conic section eccentricity (离心率) problems. The speaker solves ~30 problems almost entirely by drawing + boundary-case probing + seven-triangle recognition. The main structural message: eccentricity range questions are solved by finding two extreme configurations, not by algebraic inequality manipulation.

### Segment: First 30 Minutes

Content: Eccentricity basics — ellipse can be horizontal or vertical, sign traps, 3-4-5 triangle recognition, rectangle property (对角线相等且平分), cosine-law usage.

Mechanism signals:

1. `Answer-Shape Pressure` — **strong, direct**
   - "错误答案重合的地方中间就正确的答案" → error-convergence heuristic for multiple-choice
   - "单个的就错了" → missing a sign case eliminates two options at once

2. `Function Archetype Matching` — **strong, direct**
   - "ABC呢是个直角三角形又是A加C等于二B只有两个三角形一个是等边三角形一个是三是五三角形"
   - instant recognition of 3-4-5 from the constraint

3. `Draw-First Decoding` — **strong, pervasive**
   - "永远我做的一件事是画图" — every problem starts with drawing
   - "一定把另一个焦点画出来" — always draw F' even if only F is given

### Segment: 30-60 Minutes

Content: Eccentricity range problems using boundary-case probing, rectangle property, cosine law in focal triangles.

Mechanism signals:

1. `Boundary As Route Finder` — **strong, direct, systematic**
   - "边界点和对称点不到极值范围吗" → two boundary configurations determine the range
   - method: find eccentricity at vertex-coincidence case + at infinity case → range endpoints
   - "我直接找两个极限两个极端把两个算出来然后就好了"

2. `Function Archetype Matching` — **strong, direct, repeated**
   - "见四了我就拆35" → see 4, immediately split into 3-5 (3-4-5 triangle)
   - "出现四了把它拆三五" → archetype-triggered decomposition
   - "8A时候那345我直接把它找出来了"

3. `Projection Readout` — **strong, direct**
   - "PF2的平方等于PF2乘QF2就意味着它的投影就他自己所以直角是90度"
   - dot-product self-equality → right angle readout

### Segment: 60-95 Minutes

Content: Complex eccentricity problems — incenter via Mercedes theorem, affine-compression angle inequality, reflection-tangent problem.

Mechanism signals:

1. `Model Calling Before Derivation` — **strong, direct**
   - "奔驰定理" (Mercedes/barycentric theorem): weighted vertex vectors sum to zero at incenter
   - "内切圆因为面积嘛面积二分之底乘高高的相等所以就成了边的相等比" → area ratio = edge ratio
   - "知道奔驰定理以后立刻得分不知道的话现在把人给累死"

2. `Boundary As Route Finder` — **strong, direct**
   - affine compression argument: "零的时候验证最大时候才90度那别的永远不可能到90度" → circle is the max-angle case, ellipse can only be smaller
   - "仿射变换再压扁就变得更小了所以他铁定性角数"

3. `Answer-Shape Pressure` — **strong, direct**
   - reflection tangent: "斜率是正的排除两个负的" → sign eliminates 2 options
   - "一的时候就只差圆心还怎么相切" → slope=1 passes through center, impossible tangency → eliminates another
   - "所以答案只能1/8"

4. `Boundary As Route Finder` — **strong, summary**
   - "E等于零的时候表示C等于零所以A等于B这个时候变成一个圆"
   - "E等于一的时候表示A等于C B等于零所以椭圆就成一根棍儿了"
   - "E等于无穷大的时候就是C比A是无穷大就成了无限贴近于Y轴"
   - these degenerate cases are used as universal problem-solving anchors

### Episode-Level Judgment For `BV15VdUBPETA`

After full-episode extraction, this episode strongly reinforces:

- `Boundary As Route Finder` (two-endpoint boundary method as the primary strategy for eccentricity range)
- `Function Archetype Matching` (see-4-split-3-5, seven triangles as instant recognition vocabulary)
- `Draw-First Decoding` (every problem solved by drawing; always draw the missing focal point)
- `Answer-Shape Pressure` (multi-criteria elimination: sign, bracket type, impossibility)

And gives additional support to:

- `Projection Readout` (dot-product self-equality → right angle)
- `Model Calling Before Derivation` (Mercedes theorem for incenter)
- `Canonical Normalization` (eccentricity boundary cases as universal anchors)

The main drop-dimension axis of this episode is:

- eccentricity range problems are not inequality problems but boundary-configuration problems
- the two-endpoint method (vertex coincidence + point at infinity) replaces algebraic range analysis
- the seven special triangles drive instant recognition even in complex focal-triangle setups
- small problems must never be done as big problems ("小题不大做" as first iron law)

---

## Episode: `BV1eVdUBPEGj` (ep026, 89:36)

This episode covers circles and lines (圆和直线). The speaker solves ~30 problems using drawing, parameter consolidation for fixed points, perpendicular-line intersection circles, tangent formulas, and seven-triangle recognition. The structural message: circle-line problems are entirely graphical — draw first, recognize triangles, read answers.

### Segment: First 30 Minutes

Content: Set intersection via drawing, parameter consolidation to find fixed points (合并同类项 → 过定点), special-value probing, confidence-building.

Mechanism signals:

1. `Canonical Normalization` — **strong, direct, systematic**
   - "合并同类项因为什么呢它过定点的时候A的系数为零就过定点"
   - technique: rewrite parametric family of lines so parameter multiplies one term; set coefficient to zero → fixed point
   - repeated for every "过定点" problem in the episode

2. `Special-Value Probing` — **strong, direct**
   - "相等零无穷大是我最喜欢找的三个特例三块石头"
   - the three canonical special values form a universal probing toolkit
   - "找特例呗我让每个都是一就不满足了"

3. `Answer-Shape Pressure` — **strong, direct**
   - "错误答案重合的地方中间就正确的答案" → error-convergence heuristic
   - "特例对的不一定对但特例错的一定错" → falsification logic

### Segment: 30-60 Minutes

Content: Two perpendicular lines through fixed points trace a circle, tangent line shortest distance, seven-triangle angle recognition.

Mechanism signals:

1. `Projection Readout` — **strong, direct**
   - perpendicular lines through two fixed points: slopes k and -1/k → intersection traces a circle (AB as diameter)
   - "最大的面积显然是正好垂直时候" → perpendicularity gives extremum

2. `Function Archetype Matching` — **strong, direct, repeated**
   - 1-3-√10 triangle: "E3根号十是18.5度" → doubled = 37° → 3-4-5 triangle
   - "高考只考七个三角形" reiterated with full interconnection chain
   - "我知道是我的三角形我就很开心我就很有信心"

3. `Definition As Direct Readout` — **strong, direct, systematic**
   - unified tangent formula across all conics: x² → x₀x, y² → y₀y
   - "切线圆是这样椭也这样抛物线也是双曲线呢符号呗一样的"
   - chord-of-contact equation: same substitution rule for interior points

### Segment: 60-90 Minutes

Content: More tangent problems, shortest chord via perpendicular, fixed-point discovery via focal-directrix analogy, answer-guessing strategy.

Mechanism signals:

1. `Boundary As Route Finder` — **strong, direct**
   - circle fixed-point discovery via focal-directrix analogy: "把这变换压扁了准线A方除以C" → treats circle as degenerate ellipse to find the fixed point of tangent chords
   - "原学大题用这个猜答案一猜一个准"

2. `Answer-Shape Pressure` — **strong, direct, systematic**
   - multi-criteria elimination: bracket type (方括号 vs 圆括号), symmetry, error convergence
   - "前方后圆前圆后方方方方方这应该是方方方方" → bracket pattern heuristic
   - "这个猜答案是92%的正确率" → quantified confidence in elimination strategy

3. `Draw-First Decoding` — **strong, pervasive**
   - "我以多半时间在画图" → more than half the time is spent drawing
   - "高考图都是准的我直接量一下" → physical measurement on exam figures as valid strategy

### Episode-Level Judgment For `BV1eVdUBPEGj`

After full-episode extraction, this episode strongly reinforces:

- `Canonical Normalization` (parameter consolidation → fixed point discovery as universal technique)
- `Draw-First Decoding` (every problem solved by drawing; physical measurement on exam figures)
- `Function Archetype Matching` (seven triangles with interconnection chain)
- `Answer-Shape Pressure` (bracket type + symmetry + error convergence = 92% elimination)

 And gives additional support to:

 - `Special-Value Probing` (three canonical special values: equal, zero, infinity)
 - `Definition As Direct Readout` (unified tangent formula across all conics)
 - `Projection Readout` (perpendicular-line intersection traces circle)
 - `Boundary As Route Finder` (focal-directrix analogy for circle fixed points)

 The main drop-dimension axis of this episode is:

 - circle-line problems are entirely graphical; parameter consolidation reveals fixed points
 - two perpendicular parametric lines through fixed points → intersection traces a circle → extremum at perpendicular
 - the unified tangent formula (x²→x₀x, y²→y₀y) applies identically to circle, ellipse, parabola, hyperbola
 - answer-guessing by elimination is quantifiably better (92%) than random guessing (25%)

 ---

 ## Episode: `BV1h7dUBhEYk` (ep028, 92:24)

 This episode is devoted to reformed 15-point solid geometry. The speaker’s main claim is that the topic did not get easier after reform: the extra 3 points push many problems out of the old “set coordinates and grind” regime. The stable route is to mark every condition on the figure, redraw messy 3D relations as plane slices, try midpoint or trisection guesses, and prefer equal-volume or projection methods before coordinates.

 ### Segment: First 30 Minutes

 Content: Reform commentary, score strategy, first hard fold problem, and what to do when the first proof stalls.

 Mechanism signals:

 1. `Draw-First Decoding` — **strong, direct, systematic**
    - "把所有条件标上来"
    - "把条件标到图上"
    - "不能多不能少" → do not add fake perpendicularity, regularity, or symmetry that the statement never gave

 2. `Low-Floor Ownership` — **strong, direct**
    - "第一问证不出来你假装证出来了 接着做第二问"
    - "第二问不会做不妨碍你做第三问"
    - preserve score flow instead of emotionally freezing on the first obstruction

 3. `Projection Readout` — **strong, direct**
    - "立体几何不好看的时候才做平面几何"
    - "不清楚的时候画个平面图"
    - flatten 3D mess into a 2D triangle where cosine law, area, and special triangles become readable

 ### Segment: 30-60 Minutes

 Content: Line-plane angle via equal-volume, standard prism/trapezoid problems, and midpoint section arguments.

 Mechanism signals:

 1. `Projection Readout` — **strong, direct, repeated**
    - "最好先用等体积法 不要解析"
    - line-plane angle or point-plane distance is reduced to one height or one area ratio instead of full coordinates

 2. `Function Archetype Matching` — **strong, direct**
    - 1-2-√5, 1-1-√2, and equilateral triangles are repeatedly recognized inside 3D sections
    - "高考七个三角形" remains the fast controller vocabulary even in solids

 3. `Symmetry As Variable Killer` — **strong, direct**
    - midpoint and center-line structures are repeatedly turned into midlines, heights, and parallel relations
    - the unknown point is often forced by a symmetric section before any calculation starts

 ### Segment: 60-92 Minutes

 Content: Slanted prisms and slanted tetrahedra, coordinate-axis selection from real perpendicular edges, repeated midpoint probing.

 Mechanism signals:

 1. `Symmetry As Variable Killer` — **strong, direct, tactical**
    - "终点是大问题的答案"
    - "不是终点就三分之一三分之二点"
    - midpoint-first probing collapses the search for constructed points in 3D

 2. `Projection Readout` — **strong, direct**
    - "投影太重要了 立刻知道我解析的东西了"
    - the projection foot or actual perpendicular edge decides where the coordinate axes should be built

 3. `Draw-First Decoding` — **strong, summary**
    - "发现解不出来之后 看看哪个条件没有用"
    - route failure is treated as a signal that one marked condition has not yet been consumed

 ### Episode-Level Judgment For `BV1h7dUBhEYk`

 After full-episode extraction, this episode strongly reinforces:

 - `Draw-First Decoding` (every usable relation must be marked on the figure; neither more nor less)
 - `Projection Readout` (plane slice, equal-volume, and real projection beat raw coordinates)
 - `Symmetry As Variable Killer` (midpoint / trisection probing collapses point search in 3D)

 And gives additional support to:

 - `Function Archetype Matching` (seven special triangles still control solid geometry)
 - `Low-Floor Ownership` (if the first proof jams, keep harvesting downstream points)

 The main drop-dimension axis of this episode is:

 - reformed solid geometry is a route-control problem, not a coordinate reflex problem
 - the picture must consume every condition exactly once; unused conditions signal the missing move
 - equal-volume and plane slicing replace much of the old analytic grind
 - midpoint-first probing is the default fast search on constructed 3D points

 ---

 ## Episode: `BV137dUBhE6A` (ep029, 98:19)

 This episode is devoted to new-definition and innovation problems. The structural message is that most “创新题” are not solved by bravely grinding the new notation. They are solved by first deciding whether the shell is fake or real, then materializing the definition with a diagram or examples, then counting, finding patterns, or abandoning the full solve while still salvaging points.

 ### Segment: First 30 Minutes

 Content: Taxonomy of innovation questions, pseudo-innovation vs real innovation vs killer questions, first set-definition examples.

 Mechanism signals:

 1. `Shell-Kill` — **strong, direct**
    - "伪创新题"
    - "只是套了个创新的壳儿"
    - "说穿了剩下的都是纯粹的数学"

 2. `Definition As Direct Readout` — **strong, direct**
    - "审题审题审题"
    - "一个字一个字读明白"
    - the first task is to identify exactly who is defined, what operation is defined, and what the question is actually asking

 3. `Special-Value Probing` — **strong, direct**
    - "画图和特例 这一步是最重要的"
    - "要么画图 要么找几个例子"
    - abstract definitions are made concrete through one picture or one small witness set

 ### Segment: 30-60 Minutes

 Content: Cartesian-product set definitions, set-system counting, and direct enumeration under new notation.

 Mechanism signals:

 1. `Count-Do-Not-Derive` — **strong, direct, systematic**
    - "数问题是数得出来的"
    - "数出来"
    - list actual configurations and small cases instead of opening with formal derivation

 2. `Definition As Direct Readout` — **strong, direct**
    - the transcript repeatedly stops to ask who the universe is, who the subset is, and what exactly the new relation permits
    - object-role parsing comes before all calculation

 3. `Special-Value Probing` — **strong, direct**
    - tiny concrete sets like `A={1,2}`, `B={2,3}` are used to decode the operation itself before any general reasoning

 ### Segment: 60-98 Minutes

 Content: Fibonacci and transformed sequences, strange functions, Newton-iteration shells, Hölder-like definitions, and multiple-choice rescue strategies.

 Mechanism signals:

 1. `Count-Do-Not-Derive` — **strong, direct**
    - early-term calculation (`A1`, `A2`, `A3`, ...) is used to expose pattern and kill fake formula derivation
    - weird recurrences are solved by listing and reading, not by opening with closed forms

 2. `Answer-Shape Pressure` — **strong, direct**
    - "最长最长最复杂那个一般是对的"
    - when full resolution is too expensive, eliminate obvious impossibilities first and then use structural answer-shape pressure to salvage points

 3. `Shell-Kill` — **strong, direct**
    - many apparently exotic problems are re-expressed as ordinary graph, derivative, or geometry problems once the shell is removed
    - the novelty often lives in the wording, not in the controller mathematics

 ### Episode-Level Judgment For `BV137dUBhE6A`

 After full-episode extraction, this episode strongly reinforces:

 - `Definition As Direct Readout` (new definitions must be parsed exactly before anything else)
 - `Special-Value Probing` (diagram/example witnesses are the fastest way to make the definition concrete)
 - `Count-Do-Not-Derive` (small-case counting and early-term listing beat full derivation)
 - `Answer-Shape Pressure` (innovation multiple-choice can still be mined for points under time collapse)

 And gives additional support to:

 - `Shell-Kill` (distinguish pseudo-innovation shell from real conceptual novelty)

 The main drop-dimension axis of this episode is:

 - innovation questions are first language-decoding problems, then mathematics problems
 - the correct first move is almost always to materialize the definition with a picture or example
 - if the shell is fake, strip it and route back to ordinary math immediately
 - if the shell is real and time is collapsing, keep the score floor through counting, partial elimination, and controlled guessing

---

## 80-93 Minute Extraction Notes (`BV1BNdUBzE3E`)

Working source:

- [ep011_BV1BNdUBzE3E.txt](c:/Users/21604/Desktop/niu/.tmp/bili_watch/episodes/ep011_BV1BNdUBzE3E.txt)

### Observed mechanism signals

1. `Projection Readout` strengthened through direct triangle-law calling, projection theorem, and immediate special-triangle drawing
2. `Special-Value Probing` remains dominant in conic, solid, and vector big-question shells
3. `Canonical Normalization` support through legal translation / flipping / rotating when the asked quantity is rigid-motion invariant
4. `Answer-Shape Pressure` support through upper/lower witness worlds and direct option elimination

### Immediate extraction judgment

The final `80-93` minute section turns the earlier “toy world first” route into a harder exam version:

- first call a stored triangle/controller law or an invariant-preserving rewrite
- then freeze a symmetric or extreme special case so the answer family becomes visible
- only after the answer direction is obvious, backfill the formal shell if the exam still asks for it

So this segment mainly reinforces:

- `Projection Readout`
- `Special-Value Probing`
- `Canonical Normalization`
- `Answer-Shape Pressure`

### Episode-Level Judgment For `BV1BNdUBzE3E`

After full-episode extraction, this episode strongly reinforces:

- `Special-Value Probing`
- `Model Calling Before Derivation`
- `Projection Readout`

And gives additional support to:

- `Canonical Normalization`
- `Guess Then Verify`
- `Answer-Shape Pressure`
- `Rapid Verification`

The main drop-dimension axis of this episode is:

- when conditions are sparse or the shell is too expensive, immediately build the cheapest legal world
- let that world expose family, answer direction, or impossibility before symbolic grind
- if the asked quantity survives rigid repositioning, normalize the figure first and solve there

## 40-60 Minute Extraction Notes (`BV17NdUBzE73`)

Working source:

- [ep012_BV17NdUBzE73.txt](c:/Users/21604/Desktop/niu/.tmp/bili_watch/episodes/ep012_BV17NdUBzE73.txt)

### Observed mechanism signals

1. `Symmetry As Variable Killer` remains dominant in inequality extremum questions
2. `Canonical Normalization` strengthened via coefficient rebalancing and constant-sum rewriting
3. `Rapid Verification` support through asymmetric/extreme witnesses that distinguish minimum from mere range information
4. `Model Calling Before Derivation` support through immediate routing into Cauchy / full-square / AM-GM templates once the shell is balanced

### Immediate extraction judgment

This `40-60` minute section treats ugly inequalities as coefficient-puzzle engineering:

- do not attack the original fractions directly
- first rescale, split, or add the missing constant until the shell becomes balanced
- once the coefficients line up, the extremum often becomes one stored inequality template away

So this segment mainly reinforces:

- `Symmetry As Variable Killer`
- `Canonical Normalization`
- `Model Calling Before Derivation`
- `Rapid Verification`

## 60-80 Minute Extraction Notes (`BV17NdUBzE73`)

Working source:

- [ep012_BV17NdUBzE73.txt](c:/Users/21604/Desktop/niu/.tmp/bili_watch/episodes/ep012_BV17NdUBzE73.txt)

### Observed mechanism signals

1. `Rapid Verification` strengthened through simultaneous-equality checking across chained inequalities
2. `Symmetry As Variable Killer` remains dominant through direct `A=B`, `B=C`, and `A=C` freezes
3. `Canonical Normalization` support via constant splitting and variable rewrites that make the shell balanceable
4. `Special-Value Probing` support through equality-attainable witness states and cheap range checks

### Immediate extraction judgment

The `60-80` minute segment adds a crucial honesty gate to inequality solving:

- a lower bound is not yet the answer just because each step looks standard
- the equality bills from all sub-inequalities must be payable by the same variable state
- once that shared state exists, symmetry becomes the fastest way to land the extremum cleanly

So this segment mainly reinforces:

- `Rapid Verification`
- `Symmetry As Variable Killer`
- `Canonical Normalization`
- `Special-Value Probing`

## 80-94 Minute Extraction Notes (`BV17NdUBzE73`)

Working source:

- [ep012_BV17NdUBzE73.txt](c:/Users/21604/Desktop/niu/.tmp/bili_watch/episodes/ep012_BV17NdUBzE73.txt)

### Observed mechanism signals

1. `Projection Readout` strengthened by drawing the area picture and turning it into a sum constraint
2. `Answer-Shape Pressure` support through single-choice elimination after three candidates are killed
3. `Special-Value Probing` remains active through achievable endpoint/range witnesses
4. `Symmetry As Variable Killer` support through final substitutions that collapse the remaining variables

### Immediate extraction judgment

The final `80-94` minute section returns the same bottom rule in exam form:

- draw first whenever the geometry shell hides a cheap conservation law
- for single-choice or range questions, a few legal witnesses can be enough; after three options die, stop paying for a full derivation
- when the algebra is still ugly, keep searching for the variable pair that is actually symmetric and collapse there

So this segment mainly reinforces:

- `Projection Readout`
- `Answer-Shape Pressure`
- `Special-Value Probing`
- `Symmetry As Variable Killer`

### Episode-Level Judgment For `BV17NdUBzE73`

After full-episode extraction, this episode strongly reinforces:

- `Symmetry As Variable Killer`
- `Projection Readout`
- `Canonical Normalization`

And gives additional support to:

- `Rapid Verification`
- `Special-Value Probing`
- `Answer-Shape Pressure`
- `Model Calling Before Derivation`

The main drop-dimension axis of this episode is:

- rewrite ugly inequalities until the variables fall into a balanced or symmetric controller world
- do not trust an extremum unless the equality state is actually achievable under the whole chain
- on option and range questions, cheap witnesses and picture readout should kill candidates before general derivation

---
 
## Episode Extraction Notes (`BV1sjd6BFEbg`)

Working source:

- [ep020_BV1sjd6BFEbg.txt](c:/Users/21604/Desktop/niu/.tmp/bili_watch/episodes/ep020_BV1sjd6BFEbg.txt)

### Observed mechanism signals

1. `Function Archetype Matching` dominates through direct odd/even family identification and composite-parity readout
2. `Projection Readout` support appears when `odd + constant` shells are turned into symmetry-center readout for `f(x) + f(-x)` and `max + min`
3. `Model Calling Before Derivation` support appears when periodic and half-period conditions are treated as reusable shells instead of fresh algebra every time
4. `Draw-First Decoding` support remains active through graph-based reading of symmetry, interval midpoint, and transformed function behavior

### Immediate extraction judgment

This episode keeps compressing function-property problems into a tiny family catalogue:

- do not re-open the definition from zero on every question
- first decide whether the shell is really odd, even, odd-plus-constant, or a shifted/scaled periodic family
- once the family is fixed, read center, extremum relation, and symmetry consequences from the graph carrier directly
- if the condition has the shape `f(x+a)=-f(x)` or `f(x)+f(x+a)=const`, treat it as half-period information before paying for algebra

So this episode mainly reinforces:

- `Function Archetype Matching`
- `Projection Readout`
- `Model Calling Before Derivation`
- `Draw-First Decoding`

### Episode-Level Judgment For `BV1sjd6BFEbg`

After full-episode extraction, this episode strongly reinforces:

- `Function Archetype Matching`
- `Projection Readout`
- `Draw-First Decoding`

And gives additional support to:

- `Model Calling Before Derivation`

The main drop-dimension axis of this episode is:

- collapse function-property questions into a small graph-family vocabulary first
- once a symmetry center or periodic shell is recognized, read consequences there instead of restarting symbolic derivation
- treat half-period and odd-plus-constant relations as stored controllers, not as one-off tricks

## Episode Extraction Notes (`BV1eVdUBPEKD`)

Working source:

- [ep025_BV1eVdUBPEKD.txt](c:/Users/21604/Desktop/niu/.tmp/bili_watch/episodes/ep025_BV1eVdUBPEKD.txt)

### Observed mechanism signals

1. `Draw-First Decoding` is dominant: transformed trigonometric shells are repeatedly solved by redrawing the wave before any parameter algebra
2. `Projection Readout` support is strong through direct readout of period, shift, symmetry axis, overlap point, and extremum location from the graph
3. `Model Calling Before Derivation` support appears whenever the speaker compresses the shell back into one standard sine/cosine wave under translation, scaling, and reflection
4. `Answer-Shape Pressure` support appears because a quick sketch kills impossible parameter ranges and option shapes long before a full derivation is finished

### Immediate extraction judgment

This episode treats trigonometric parameter problems as graph-operator problems first:

- materialize one honest period, one anchor point, and the symmetry axis before solving equations
- left/right shifts, stretch, and sign flip should first be seen as geometric moves on the wave
- once the picture is stable, parameters are usually readable from overlap, zero, peak, trough, or axis information
- only then backfill the exact algebra if the exam still requires it

So this episode mainly reinforces:

- `Draw-First Decoding`
- `Projection Readout`
- `Model Calling Before Derivation`
- `Answer-Shape Pressure`

### Episode-Level Judgment For `BV1eVdUBPEKD`

After full-episode extraction, this episode strongly reinforces:

- `Draw-First Decoding`
- `Projection Readout`
- `Model Calling Before Derivation`

And gives additional support to:

- `Answer-Shape Pressure`

The main drop-dimension axis of this episode is:

- trigonometric parameter questions should be externalized into a wave picture immediately
- once period and symmetry are on the page, most parameters are read rather than solved
- graph sanity should kill impossible answer families before symbolic cleanup

## Episode Extraction Notes (`BV137dUBhEiF`)

Working source:

- [ep027_BV137dUBhEiF.txt](c:/Users/21604/Desktop/niu/.tmp/bili_watch/episodes/ep027_BV137dUBhEiF.txt)

### Observed mechanism signals

1. `Count-Do-Not-Derive` dominates probability/statistics shells through explicit state-tree drawing and direct branch counting
2. `Puzzle Assignment` remains active when step-`n` probability or expectation is recoded as a sequence and normalized into a geometric-like recurrence
3. `Most-Constrained-First` support appears in stopping-rule match problems where the last round is forced by the terminal condition and only the free prefix still needs counting
4. `Model Calling Before Derivation` support appears through the repeated Markov/state-flow interpretation before any formula pushing

### Immediate extraction judgment

This episode pushes one consistent probability-process route:

- first draw the state tree and count the low-floor branches honestly
- if the next-step chance depends on the current holder/state, define the asked probability as a sequence and normalize the recurrence
- if the process stops on first attainment of a score target, freeze the forced last round first and count only the admissible prefixes
- once the small distribution is visible, expectation becomes a direct weighted readout instead of a new hard problem

So this episode mainly reinforces:

- `Count-Do-Not-Derive`
- `Puzzle Assignment`
- `Most-Constrained-First`
- `Model Calling Before Derivation`

### Episode-Level Judgment For `BV137dUBhEiF`

After full-episode extraction, this episode strongly reinforces:

- `Count-Do-Not-Derive`
- `Puzzle Assignment`
- `Model Calling Before Derivation`

And gives additional support to:

- `Most-Constrained-First`
- `Draw-First Decoding`

The main drop-dimension axis of this episode is:

- probability processes are first state-graph problems, not formula problems
- when memory exists, write the state probability as a sequence and normalize it before solving
- when a stopping rule fixes the last event, count the free prefix only and read expectation from the resulting small distribution

## Season-Level Synthesis (Visible 21-Episode Batch)

### Dominant repeated signals

1. `Draw-First Decoding` + `Projection Readout`
   - this is the most stable season pair
   - `Draw-First Decoding` is strongly reinforced in `12` episode-level judgments
   - `Projection Readout` is strongly reinforced in `11` episode-level judgments
   - geometry, conics, trigonometric graphs, and even process-style probability questions are repeatedly externalized first and only solved second

2. `Function Archetype Matching` + `Model Calling Before Derivation`
   - the season repeatedly kills fake novelty by mapping shells back to known families, graph types, triangle catalogues, or process models
   - `Function Archetype Matching` is strongly reinforced in `9` episode-level judgments
   - `Model Calling Before Derivation` is strongly reinforced in `7` episode-level judgments

3. `Special-Value Probing` + `Boundary As Route Finder` + `Guess Then Verify`
   - when the full solve is not yet cheap, the route first touches seam cases, witnesses, endpoint structures, or answer-shaped filters
   - `Special-Value Probing` is strongly reinforced in `7` episode-level judgments
   - `Boundary As Route Finder` and `Guess Then Verify` are each strongly reinforced in `6` episode-level judgments

4. `Count-Do-Not-Derive` + `Most-Constrained-First`
   - combinatorics and probability shells are repeatedly handled by direct structural counting and forced-order placement instead of formula theater
   - `Count-Do-Not-Derive` is strongly reinforced in `6` episode-level judgments
   - `Most-Constrained-First` appears more often as a support controller than as the main headline primitive, but the route is stable whenever stopping conditions or tight placement rules appear

### Stable season route

Across the visible batch, the stable route is:

- externalize the honest carrier first: figure, graph, state tree, or witness example
- compress the shell into a known family, model, or controller vocabulary before local derivation
- read boundary, symmetry, special-point, or forced-order information before committing to heavy algebra
- only backfill symbolic closure if the exam still demands it

### Promotion judgment

This season now gives strong repeated support to:

- `Draw-First Decoding`
- `Projection Readout`
- `Function Archetype Matching`
- `Model Calling Before Derivation`
- `Special-Value Probing`
- `Boundary As Route Finder`
- `Count-Do-Not-Derive`

It also gives stable supporting evidence to:

- `Guess Then Verify`
- `Answer-Shape Pressure`
- `Canonical Normalization`
- `Most-Constrained-First`
- `Definition As Direct Readout`
- `Symmetry As Variable Killer`

### Season-Level Integration Judgment

Season `7950058` should now be treated as:

- a broad reinforcement lane for `geometry-first`, `graph-first`, `model-calling`, and `boundary/witness-first` controllers
- not mainly a source of many brand-new primitives
- valuable because the same low-floor route compression recurs across conics, trigonometric functions, counting, probability, space geometry, and new-definition shells

## Cross-Lane Comparison Against Existing Wu Boshi Sources

### Already independently supported outside season `7950058`

1. `Draw-First Decoding`
   - already has independent non-season anchors in `七个三角形种子线` and `三角函数平移变换 / 高次函数图像种子线`
   - season `7950058` therefore acts as dense reinforcement rather than origin evidence

2. `Function Archetype Matching`
   - already aligns with the `13个超纲函数 / 十大同构母函数` lane and the trigonometric / higher-order graph lane
   - the season strengthens this controller across conics, trigonometric graphs, and compact function shells

3. `Model Calling Before Derivation`
   - already has non-season support through the function-bank lane and the existing PDF model-calling cluster
   - the season mainly broadens the surface range where the same controller fires

4. `Count-Do-Not-Derive` and `Most-Constrained-First`
   - both already have direct non-season support in `直接数出答案种子线`
   - season `7950058` adds multi-topic transcript reinforcement, especially by extending the route from counting shells into probability-process shells

### Supported outside season, but still needing stronger non-season anchors

1. `Special-Value Probing` and `Guess Then Verify`
  - these already align with `先猜后证种子线`
  - current public search now yields phrase/search-index confirmation plus a surfaced Wu-linked public page around `BV1h642137QJ`, but still not a stable bank of clean direct episode pages
  - current judgment:
    - `cross-lane support exists, but direct non-season public exemplars remain the weakest point`

2. `Boundary As Route Finder`
  - this already aligns with `导数五类 / 六类种子线`, `圆锥曲线 / 椭圆四个点种子线`, and the PDF boundary cluster
  - direct public leads are now visible through `BV1vD421M7cY` (`圆锥曲线题直接用图解`) and the derivative-family titles around `BV1rC411x7fP` / `BV15C41147hQ`
  - current judgment:
    - `cross-lane support exists and direct public leads are visible, but a cleaner dedicated boundary-first episode bank is still missing`

3. `Projection Readout`
  - direct public leads are now visible through `BV1vD421M7cY` (`圆锥曲线题直接用图解`), `BV1WD421E7RY` (`三角函数含抽象函数平移变换保姆级教程`), and `BV15C41147hQ` (`高次函数图像怎么画？`)
  - this is stronger than pure concept-level compatibility because the public titles themselves already expose graph/figure readout as the teaching surface
  - current judgment:
    - `cross-lane support exists and direct public leads are visible, but a dedicated projection-readout lane is still thinner than the strongest branded lanes`

### Cross-Lane Judgment

The visible season batch should therefore be treated as:

- a high-density reinforcement pack for already active Wu Boshi controllers
- especially strong for `geometry-first`, `graph-first`, `function-bank/model-calling`, `boundary/witness-first`, and `direct-count` worlds
- not sufficient by itself to justify brand-new primitive promotion unless the weaker-anchored controllers receive stronger outside-season support
