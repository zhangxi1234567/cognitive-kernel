# Wu Boshi Evidence-Layer Corpus Status

Role note:

- this file tracks the state of the public evidence corpus
- it measures source coverage, not the full boundary of the method engine
- an exam-heavy corpus can still support a level-agnostic method if the extracted primitives transfer cleanly

This file records the current evidence status of the public corpus.

## Summary

The corpus is now structurally mature but still source-thin.

It has enough evidence to support:

- the core method families
- the cross-family tactic tree
- the generalization logic
- the skill's "降维打击学习" positioning

It does not yet have enough public evidence to fully recover:

- the contents of `13个超纲函数`
- the contents of `十大同构母函数`
- the exact `五/六大类型` subtype list
- a full direct-page set for `先猜后证`

## Strongly Supported Families

- `七个三角形`
- `尺子量法 / 比大小 / 画图量出来`
- `高考白痴题 / 降维打击`
- `先猜后证 / 直接出答案 / 数出答案 / 量出答案` as a recurring phrase family

## Moderately Supported Families

- `13个超纲函数`
- `十大同构母函数`
- `五/六大类型`
- `椭圆四个点`
- `漫画 / 故事 / 动画` as broad themed pedagogy

## Weakly Supported Details

- exact list members inside compression families
- direct per-video links for all phrase variants
- explicit game mechanics as pedagogy

## Decision

Broad public scraping is now near diminishing returns.

The best next move is no longer:

- collect more generic public pages

The best next move is:

- strengthen the skill's internal solving protocol
- turn the evidence into higher-quality generalization behavior
- optionally do very narrow follow-up searches only when a specific sparse family blocks quality

Boundary reminder:

- current corpus density is strongest around public exam-facing artifacts because those are easiest to observe
- that source shape should guide evidence priorities, not redefine the method as "only for gaokao / school math"
