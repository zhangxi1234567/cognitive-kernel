# Native Consumption Boundary

This file does not define hooks.

If a host needs named hooks to behave more natively,
the surface is still too explicit.

## Final Rule

Progress toward native behavior should reduce visible interface,
not rename it.
