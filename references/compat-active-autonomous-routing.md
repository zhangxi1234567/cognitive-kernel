# Compatibility Boundary Note

This document no longer defines active autonomous routing.

## Hard Rule

Do not expose routing fields, candidate sets, or competition objects as runtime-facing structures.

Autonomy must remain a live phenomenon, not a named routing surface.
