# Wu Boshi Public Function-Bank Registry

This file is a structured placeholder-registry for the public series lines:

- `13个超纲函数`
- `十大同构母函数`
- `六大完美超越函数`

Current public surfaces confirm the existence of these banks more strongly than they reveal the full member lists.
So this registry stores:

- what the bank is trying to do
- what each eventual member entry must contain
- what is already known
- what is still blocked on fuller transcript/video access

## Registry Schema

For each future bank entry, record:

1. `Public bank name`
2. `Member label`
3. `Recognition cue`
4. `Behavior family`
5. `Default primitive`
6. `Common bait route`
7. `Cheapest faithful seal`
8. `Cross-level note`
9. `Cross-domain analogue`
10. `Evidence status`

## 1. 13个超纲函数

### Known from public surface

- this is presented as a finite bank
- the pedagogical promise is: broad topic -> small list -> route by recognition
- the likely extraction target is not “13 names” alone, but “13 behavior families or wrappers”

### What each member should become

- a routing entry in `topic-routing-maps`
- a function-bank recognition card
- a default primitive suggestion

### Current status

- `public existence: confirmed`
- `full member list: unresolved`
- `method meaning: partially integrated`

## 2. 十大同构母函数

### Known from public surface

- this is presented as a mother-pattern dictionary
- the promise is: many functions are disguises of a small number of mothers
- likely extraction target:
  - normal form
  - inherited behavior
  - transformation family

### What each member should become

- one mother-pattern card
- one normal-form description
- one transformation map
- one bait-route warning

### Current status

- `public existence: confirmed`
- `full member list: unresolved`
- `method meaning: strongly suggested`

## 3. 六大完美超越函数

### Known from public surface

- this appears to be another finite behavior bank
- likely role:
  - trigger recognition
  - over-standard wrapper stripping
  - route compression inside function topics

### What each member should become

- one recognition pattern
- one shell-kill description
- one default route primitive

### Current status

- `public existence: partially confirmed`
- `full member list: unresolved`
- `method meaning: moderately suggested`

## Hard Rule

Do not fill unknown member lists by guesswork.

Until fuller evidence exists, integrate:

- the bank logic
- the routing promise
- the extraction schema

but keep unresolved members explicitly marked unresolved.
