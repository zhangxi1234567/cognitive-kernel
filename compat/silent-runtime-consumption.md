# Silent Runtime Consumption

Silent runtime consumption is not a hidden control plane.

It is better behavior with fewer visible objects.

## Final Rule

If the host improves by consuming less explicit structure,
that is progress.
If it improves by consuming a renamed control surface,
that is regression.
