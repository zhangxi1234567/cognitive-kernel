# Compat-Only Active Boundary

This file is no longer an active-surface index.

Compat-only use should remain thin enough that the host does not acquire a hidden solve-time library.

## Final Rule

If compat-only needs an expanded active surface to work,
it has already drifted.
