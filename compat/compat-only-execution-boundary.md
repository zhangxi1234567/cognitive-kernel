# Compat-Only Execution Boundary

Compat-only execution means:

- use only the thin compatibility boundary
- do not borrow wider ideal-layer breadth

## Final Rule

If a compat-only result depends on wider package spillover,
it does not count as compat-only.
