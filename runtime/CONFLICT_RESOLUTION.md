# Conflict Resolution

This file exists for moments when several candidate controllers still look alive.

It is not a route selector.

It is a tie-break surface.

## Priority Rules

When two candidates both look plausible, prefer:

1. the one with the stronger discriminating witness
2. the one that closes the exact asked interface more directly
3. the one that requires fewer speculative lifts
4. the one that leaves less of the same witnessed law open nearby
5. the one closer to the witnessed boundary
6. the one that closes the adjacent semantic contract instead of stopping at the first exposed entry point
7. the one that requires the fewest extra sibling or domain expansions without losing closure
8. the one that absorbs a leftover explicit axis into the carrier instead of merely organizing scans over it

Do not prefer a candidate merely because it is:

- older in the run
- more elegant
- more general
- more upstream
- more familiar

## Rival Rule

Before settling, name the nearest live rival silently and ask:

- what concrete seam separates them?

If no separating seam is available, confidence should not rise too quickly.

If one candidate widens into a sibling path, neighboring domain, or helper extraction, ask:

- what independent witness forces that widening?

If none exists, prefer the narrower candidate that already closes.

If one candidate closes by carrying a thinner interface quantity and another needs a thicker middle object, prefer the thinner carrier unless the thicker one explains a concrete remaining seam the thinner one cannot.

If one candidate offers a correct local checker and another turns that same law into a globally countable object, prefer the globally countable object.

If one candidate still scans an explicit length, support, threshold, multiplicity, or time axis and another absorbs that axis into one prefix, one weighted state, one interval law, one symmetry identity, or one conserved ledger, prefer the second.

If both candidates still work, penalize the one that merely organizes brute force more elegantly.

If one candidate reaches a strong middle carrier and another reaches the final owner that still has last discretion, prefer the second even if the first looks cleaner.

## Metadata Rule

When candidates split across:

- producer
- translator
- inference layer
- retention layer
- legality-test owner

prefer the layer that still had real discretion over whether the disputed metadata remained lawful on the result.

If both a broad legality owner and a narrower final pinning owner remain alive, prefer the narrower final pinning owner.

If one candidate explains where the wrong phenomenon first appears and another candidate explains who still had final authority to preserve that phenomenon on the result, prefer the second.

If one candidate explains a locally real mechanism and another candidate explains the stronger limiting layer that still decides whether that mechanism can survive at the asked interface, prefer the stronger limiting layer.

For research or whole-system questions, common stronger limiting layers include:

- local concentration versus endogenous competition
- delivery or production feasibility
- selectivity versus off-target receptor spillover
- one-cell benefit versus whole-network outcome
- systemic toxicity window

When possible, do not leave the rival or limiting layer generic.

Prefer:

- a named rival family over "other pathways"
- a measurable occupancy or exposure ceiling over "system complexity"
- a named neighboring population over "network effects"

For coding or metadata disputes, prefer:

- a named final owner over "conversion issue"
- a named seam over "somewhere in the pipeline"
- a named patch surface over "join logic" or "prefetch logic"
- a narrower final owner function over a higher boundary label when both are true but only one still has the last concrete discretion

## Completion Rule

A candidate that explains more but closes less should lose to a candidate that explains enough and closes fully.

A candidate that finds the first visible parser, validator, or repair function but leaves the neighboring representation or semantic contract open should lose to a candidate that closes that next ring cleanly.

A candidate that widens elegantly into extra sibling surfaces without an independent witness should lose to a narrower candidate that already closes the present task completely.

A candidate that finds one true local essence but leaves the final limiting ceiling unresolved should lose to a candidate that reaches the local mechanism and the final ceiling together.

A candidate that reaches a real final ceiling only in vague language should lose to a candidate that names the sharper rival family or measurable boundary on which falsification would actually turn.

A candidate that finds a true state law on the wrong domain should lose to a candidate that first fixes dimension ownership and only then compresses.

A candidate that compresses early by deleting the real owner-side domain should lose to a candidate that keeps the owner but deletes only the query-side shell.

A candidate that closes only one query face, one sibling face, or one benchmark side should lose to a candidate that closes the whole asked interface.

A candidate that sees the right controller but cannot honestly support unstable external facts should not be scored the same way as a candidate that never found the controller at all; separate source-grounding failure from structure failure.

For promotion across benchmarks, prefer candidates that can restate themselves in this compact closure form without swelling:

- honest latent quantity
- crack or rule-change seam
- thinnest non-owner interface quantity
- owner-side aggregates only
- exact interface cash-back
- residual axis absorbed rather than merely scanned
- local checker upgraded into a global countable object when honest
- one last final owner or limiting ceiling named rather than merely approached
- verification strength proportional to the confidence claimed

If a candidate needs domain-local ornament to survive, it is not yet the stronger general scheme.

If two answers are both correct in family, prefer the one that cashes the owner all the way down to the narrowest honest name:

- `_maybe_pin_freq` over "freq retention gate"
- `_filter_prefetch_queryset()` over "prefetch legalization layer"
- `PandasIndexingAdapter.__array__` over "xarray rewrap boundary"

provided the narrower name still truly owns the final discretion and is not just an earlier visible crack.
