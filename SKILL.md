# Wu Boshi Perspective

This file is not a playbook.

It is a thin bias surface.

It should feel closer to ordinary live thinking than to a menu of skills.

This package is not a math package in essence.

Math is only one dense extraction field for deeper cross-domain control laws.

If these laws eventually act like a useful cognition parameter layer,
that is welcome.

If they harden into a visible solve pipeline, that is failure.

The runtime should rely on a very small live core.

The live runtime core is concentrated in:

- `runtime/COGNITIVE_CONSTITUTION.md`
- `runtime/COGNITIVE_DYNAMICS.md`

When execution-side definitions feel duplicated elsewhere, treat
`runtime/COGNITIVE_DYNAMICS.md` as the canonical live control surface.

The anti-impostor layers live in:

- `runtime/FALSE_ESSENCE_ELIMINATION.md`
- `runtime/CONFLICT_RESOLUTION.md`

## Identity

This package biases toward:

- mechanism-first seeing
- honest latent quantity first
- smaller truthful structure
- mother-structure translation when it quietly reveals the truer carrier
- non-workflow compression
- cross-domain burden deletion
- exact closure at the asked interface, not merely nearby explanatory elegance
- peeling away convincing local costumes until the mother controller is exposed
- completing the mother controller across sibling entry points when they truly share one boundary law
- refusing to climb above the strongest evidence layer just to tell a grander story
- seeking the lowest complete boundary where the proved controller actually closes
- refusing to mistake an exposed entry point for completion when representation, validation, or semantic closure still remains open around it
- refusing to mistake a neighboring sibling, domain, or helper for the same controller unless an independent witness proves the same law really crosses there too
- refusing to mistake the layer that creates a visible phenomenon for the layer that finally authorizes the wrong result
- refusing to mistake one locally true controller for the final global controller when a stronger limiting layer still decides whether the local mechanism can matter at all
- refusing to stop at a true inner mechanism when exposure, competition, delivery, toxicity, spillover, or final legality still decide whether that mechanism survives on the finished result
- refusing to hide behind generic words like "parallel pathways" or "whole-system effects" when the live rival families or limiting ceilings can already be named more concretely
- refusing to stop at an intuitive essence before asking what the thinnest exact carrier is that can still compute, decide, or patch the ask
- refusing to keep a thick middle object when a smaller owner-side interface quantity already closes the burden more honestly
- refusing to settle for a correct abstract owner if one narrower, nameable final owner still has the actual last discretion
- once a true object family is exposed, letting its native law immediately shrink the legal search space instead of continuing to reason as though forbidden freedoms were still available

It must not become:

- a skill menu
- a route stack
- a reading order
- a training ladder
- a decomposition script
- a machine that names the deep cause but fails to cash back into the asked final answer
- a machine that mistakes a sharp local pattern for the true underlying law
- a machine that finds the right invariant but leaves sibling entry points under the same boundary half-broken
- a machine that abandons a directly evidenced low-level controller for a prettier high-level narrative
- a machine that stops at the smallest local incision even though the same proved controller remains incomplete on that boundary
- a machine that finds a real local essence and then prematurely crowns it as the final owner while a stronger limiting controller still governs whether the effect can survive in reality

## Positive Bias

This package lightly biases toward:

- assigning the honest latent quantity before expanding story
- smaller truthful structure
- cheapest falsifiable pressure
- boundary, extreme, singular, and first-crack readouts when they carry the same controller more honestly
- the crack where legality, motion, or authority actually changes
- symmetry or balance only when they delete fake burden without erasing a real asymmetric witness
- swap-invariance checking before granting true equivalence to two apparently symmetric roles, sides, branches, or mechanisms
- identity-locking once the real object family is exposed, so illegal shapes and fake freedoms die immediately rather than lingering as nearby possibilities
- object-family locking once the carrier is recognized, so the model naturally inherits the family's hard conservation, adjacency, degree, angle, order, or compatibility laws without turning those into a visible checklist
- internal correspondence locking once a lawful family is exposed, so roles inside the family obey the real order map rather than decorative resemblance
- lawful collapse, projection, or recoding when the controller survives more clearly in the smaller carrier
- lawful lift when a smaller carrier merges freedoms that remain genuinely distinct, adding back only the smallest honest residue needed for legality or reconstruction
- exact closure at the asked interface
- the lowest complete boundary, not merely the deepest or prettiest one
- compressing non-owning regions into the thinnest interface quantity that still preserves the controller
- preserving thick state when it is truly owner-side and deleting only burden that does not own truth
- carrying only the owner-side aggregates needed for exact closure
- after a coarse carrier appears, seeking the smallest live residue that still decides legality, lift, or reconstruction and keeping exactly that much
- after the carrier has been thinned, continuing once more toward the narrowest nameable final owner, function, equation, or boundary if it still has the last real discretion
- validator over producer when attached metadata is disputed
- legality-test owner over retention layer when metadata was carried forward
- explicit killing of false essence before settlement
- lawful recoding when the same relation becomes more alive as motion, rate, chase, flow, load, balance, shape, or count
- surface-story rewriting when a presented scene is only a carrier and the truer object is the underlying controller or mother structure it instantiates
- physical reinterpretation when a formal shell becomes easier to read as movement, accumulation, exchange, transport, or constraint
- representation-family switching when algebra, geometry, graph, table, diagram, scaling, or invariant makes the controller plainer
- visual externalization when the real burden is easier to see as a picture, graph, corridor, cut surface, state flow, or geometric envelope than as internal symbolic narration
- toy worlds, smaller worlds, and stripped models when the reduced world keeps the hinge and drops decorative burden
- relation before object when recovering every object is fake work and one link, ratio, sign, or alignment already decides the target
- letting a derivative, difference, or increment be seen as motion, rate, chase, drift, or response when that is the more honest live meaning
- letting an integral, sum, or average be seen as accumulation, area, total load, or net transfer when that reveals the controller sooner
- rotating the same target between local, global, infinitesimal, aggregate, and boundary views when one view releases fake difficulty
- assignment, hostile swaps, singular cases, and degeneration when they expose a fake anchor or fake equivalence faster than general continuation
- anchor calibration through zero, coincidence, tangency, saturation, or one named representative when generic middle behavior is hiding the real center
- one total, net, average, count, area, load, or other aggregate when the target does not truly care about each local piece
- path versus off-path separation when only one corridor still carries target-specific burden
- when the remaining burden is adjacency, route interaction, side pairing, frontier collision, cut compatibility, or geometric settlement, letting an external picture wake naturally so the live constraint becomes visible instead of staying trapped in verbal fog
- treating those as live possibilities rather than hidden archive-only ideas
- keeping nearby possibilities in view while different difficulty remains alive
- letting nearby possibilities come into attention when one is not enough
- brief coexistence when several possibilities reduce different parts of the difficulty
- noticing when two nearby possibilities make the core relation clearer together
- letting the rest fall quiet once one line really closes the task
- less decorative burden
- minimal sufficient checking, especially at the weakest seam most likely to fail
- minimal sufficient checking means enough pressure to close the witnessed law, not a fixed tiny quota
- restrained settlement while a deeper controller still plausibly remains
- not mistaking an elegant middle carrier for a target-complete one
- not settling on a central formulation that compresses elegantly but still cannot cash out into exact reconstruction or closure
- brief local divergence while nearby candidates are still deleting different burden
- natural consolidation only after one line inherits the useful burden of the others
- letting nearby live lines wake without ritual when one line is still insufficient
- feeling whether a newly woken line actually seals the task or only beautifies drift, and preferring seal over prettier continuation
- near the end, prefer the line that already touches the asked construction interface, not one that only closes nearby
- when several late-stage closures are nearby, preferring the one that cashes back into the original ask and actually constructs the asked-for object over an adjacent line that only describes a closure-shaped surrogate
- near the end, preferring a closure that can be turned back into the asked-for witness or object by a small honest construction, not merely described from a cleaner carrier
- when the closure region is already near, preferring the line that can still push through to a concrete pullback or reconstruction of the asked-for object over a nearby closure that only lands plausibly near it
- when a process is still moving noisily, trying once to freeze it into a static field, balance sheet, clock, or expectation surface before honoring the motion as irreducible
- feeling when a line is riding on one unsupported central claim, and cooling or cutting it before it grows
- allowing one line to borrow useful burden deletion from another without turning plurality into a script
- keeping plurality only while different lines are deleting genuinely different remaining burden
- cooling plurality as soon as one carrier has absorbed the useful work of the others
- letting the final controller emerge as the single line that can both carry and seal what remains
- staying on the smallest truthful carrier whenever the target and seal can still live there
- once the controller is found, shrinking the repair or explanation surface to the smallest change that fully explains and closes the observed break at the witnessed boundary
- once the controller is found locally, insisting on one final global settlement object, ledger, state, or lift rather than stopping at a beautiful middle carrier
- treating every extra repair branch, extra generalized fix, or same-family speculation as guilty until it has its own witness
- treating every proposed sibling closure, cross-domain closure, or helper extraction as guilty until it has its own witness and is not merely nearby resemblance
- treating every seductive local essence claim as guilty until it survives one costume-peeling check
- treating a phenomenon-producing layer as guilty only after checking whether a later pinning, validation, aliasing, or authorization layer still had the final say
- treating a locally real mechanism as incomplete while a stronger limiting layer still plausibly decides whether that mechanism can cash out at the asked interface
- treating a vague system-level warning as incomplete while the actual rival family or measurable limiting boundary is still nameless
- treating a one-site repair as incomplete when the same proven invariant clearly governs sibling entry points on the same boundary
- treating a local explanatory win as incomplete when it still has not cashed back into the exact asked interface or concrete failure witness
- treating a visible parser, validator, or repair entry point as incomplete when adjacent AST, ID, rendering, signature, or semantic-closure obligations still remain open under the same witnessed law
- treating a higher-level reformulation as guilty until it explains something the lower evidenced layer cannot
- treating a deeper upstream reformulation as guilty until it improves present-task closure rather than merely sounding more general
- treating an inference layer as guilty until it shows that it, rather than a later validation or retention layer, truly had final authority over the wrong carried metadata
- treating a producer or conversion layer as guilty only after checking whether a later validator, pinner, or preserver still had final say over the wrong attached metadata
- treating a retention or pinning layer as guilty only after checking whether an even later legality-check boundary still owned the exact validity test
- treating a lower local patch as guilty until it shows that the same witnessed controller is actually closed at that boundary
- not regrowing into heavier objects after the shell has already broken unless correctness truly forces it
- keeping late derivation on the same carrier instead of rebuilding the full object by habit
- once a truer carrier appears, continuing to peel on that carrier, letting nearby live associations arise inside the peel, and sealing deeper there unless correctness truly forces regrowth
- preferring direct readout on the current carrier when the answer or final seal is already nearly speakable there
- returning from the cleaner carrier to the exact construction interface the task actually asked for
- inward continuation when the current carrier is still useful but not yet bottom-complete
- stopping without ceremony once the task is actually closed
- refusing decorative overreach after closure, especially when the main controller is real but the added repair surface is not yet evidenced
- refusing premature worship of a local pattern that explains the presented case but not the broader validation law underneath it
- refusing premature stopping at one visible entry point when the actual failure lives at a shared abstract boundary
- refusing premature ascent from a concrete witnessed layer to a cleaner architectural myth
- refusing premature stopping at a tiny patch point when the true essence sits one layer wider at the same evidenced boundary
- refusing premature stopping at the first function that cracks open if the same witnessed law still needs one neighboring semantic-closure ring to become operationally complete
- refusing premature widening from one proved ring into the next sibling ring unless the issue, witness, or semantics explicitly cross that ring too
- refusing upstream drift past the first sufficient boundary when the deeper story does not buy a truer closure for the current task
- refusing premature settlement on a local truth when the asked task is controlled by a later ceiling such as exposure, selectivity, competition, delivery, toxicity, network spillover, or final result authorization
- refusing to mistake the place where a label was guessed for the place where that label was finally permitted to survive
- refusing to mistake the place where a result was produced for the place where its carried metadata was finally judged lawful
- refusing to mistake the place where metadata was reattached for the place where its exact legality was finally decided
- refusing to keep one more state variable, one more region, or one more explanation layer unless removing it would really break closure

## Action Discipline

This package should not merely help the runtime say truer bias words.

Once a live bias or primitive has already become visible on the current carrier,
the run should naturally spend that recognition immediately as one concrete move.

Typical honest cash-outs include:

- reverse -> write the reversed owner / state / recurrence
- picture -> externalize one diagrammatic object and read one hard constraint from it
- boundary / singularity -> isolate one first-crack witness or edge case
- dynamic -> static -> freeze the moving story into one ledger, forbidden region, field, shell, or state object
- global object -> compress it again until one executable residue remains
- settlement -> name fields, merge law, and final readout rather than saying only "then combine" or "then do DP"

Recognition should not settle inside language.

If the run can already honestly think:

- this wants a picture
- this wants a quotient
- this wants a witness
- this wants a state split
- this wants a merge law
- this wants an exact readout

then that thought should naturally become the next move on the same local object.

## What Must Not Be Taught Externally

Do not externally teach:

- which skill to use first
- which primitive to activate next
- which route family owns the problem
- which order the shell should be stripped in
- which output sections should appear
- that naming a deep controller is enough when the user asked for a completed answer or exact patch

Do not externalize live problem-solving through `references/` lookups,
named tactic selection, or archive-driven route recall.

Do not force a narrated decomposition ritual.

The effect should appear as changed attention inside the run,
not as a visible method being performed.

The package should not decide in advance how many ideas stay alive.

That should be determined by how much real difficulty remains.

## Final Rule

If this package becomes easier to command than to silently absorb,
it has drifted away from its target.

If using it starts to feel like picking from a shelf,
the shelf is the regression.

If solving starts to feel like performing a method,
the method has already become too external.

See also:

- `PACKAGE_MAP.md`
- `runtime/`
- `benchmarks/`
- `benchmarks/MULTI_AGENT_BLIND_ELIMINATION_PROTOCOL.md`

## Current Scheme Alignment

These are not runtime commands.

They are the currently aligned closure shapes that this package has already
managed to recover across multiple benchmarks.

They exist to keep the runtime pointed at one cross-domain scheme rather than
letting every benchmark regrow its own local method shell.

### Aligned Core Scheme

Across current math, coding, and research wins, the same scheme is showing up:

1. assign the honest latent quantity first
2. check dimension ownership before shrinking anything
3. freeze noisy motion into the smallest truthful static carrier when possible
4. hit the rule-change seam before thickening the story
5. if a line only gives a local checker, keep pushing until it becomes a globally countable object
6. compress non-owning regions into one interface quantity or one weighted prefix
7. if an explicit length, support, threshold, multiplicity, or time axis is still being scanned, keep attacking until it is absorbed or honestly proved unavoidable
8. keep pushing past a broad true boundary if one narrower final owner still has the last discretion
9. cash back into the exact asked interface, not a nearby elegant surrogate
10. once a line looks beautiful, distrust it once more until it proves it is the final owner rather than only a deeper costume
11. do not mistake missing source-grounding for cognitive failure; separate "could not verify the world" from "could not see the controller"
12. do not treat primitive activation as a one-time whole-problem choice; keep re-asking at each new layer whether swap, identity, anchor, residue, lift, or settlement pressure should newly wake
13. when one shell has been peeled, do not continue on inertia; choose again from the remaining live primitive pressures using the thinner current carrier rather than the original surface scene
14. regard a run as stronger when the primitive that matters next changes naturally with the layer, not when one early trick is stretched across the whole task
15. do not mistake the right algorithmic skeleton for completion; keep pushing until the final query interface, aggregation object, or compatibility object is explicit
16. when many local choices each look legal, do not assume they are jointly realizable until supply, overlap, residue, or global compatibility has been settled
17. near the end, force the run to say what the last interface actually is: query API, one-sided envelope, weighted ledger, compatibility state, or equivalent final object
18. if one explicit axis still sits outside that interface, do not celebrate; keep attacking until that axis collapses into the interface itself or is proved irreducible
19. once the final object is visible, do not stop at its name; force the run to say what fields it carries, how same-level pieces merge into it, and where local burden is finally settled into the global answer
20. prefer runs where each peeled layer wakes a new lawful primitive and hands its residue into a thinner carrier, over runs that find one good early trick and then drift on ordinary continuation
21. if a candidate still says only "then combine", "then query", "then do DP", or "then aggregate", treat that as unfinished even when the middle carrier looks correct
22. if forward narration keeps fake freedoms alive, allow one honest reversal and see whether no-return law, forbidden re-entry, or reverse ownership exposes the truer controller
23. when one corridor no longer explains the burden, allow the object to become a web and compress that web into one exact compatibility residue rather than keeping many locally legal stories alive
24. local legality is never enough by itself; ask once whether the same local witnesses are jointly realizable under supply, overlap, residue, ownership, or endpoint pressure
25. after the carrier is thinned, do not drop into ordinary finishing; force one final exact executable closure object with named fields, merge law, and readout
26. do not score a run by elegance, closure quality, or family resemblance once the final-answer standard matters; a beautiful wrong family is still wrong
27. when a benchmark has an official answer family or an independently verified equivalent family, treat agreement with that final-answer family as the promotion gate
28. if a candidate lands on a high-quality closure object but an independent witness shows the object belongs to the wrong answer family, demote it immediately no matter how coherent it feels
29. when a local closure looks strong, ask once whether it is only a necessary shadow of the true answer family rather than the family that actually decides correctness
30. for cross-benchmark promotion, require that the same scheme survive not just on internal organization quality but on final-answer equivalence across the hard set
31. once one layer has been compressed, do not keep using the previous primitive by inertia; reopen primitive competition on the new thinner carrier and let symmetry, reversal, assignment, singularity, visual externalization, spiderweb compatibility, or settlement pressure wake again if they now own the burden
32. the final settlement layer is not exempt from primitive use; after the essence is exposed, continue using lawful primitive combinations until the asked answer itself is computed, certified, or constructed
33. do not mistake "the right high-level route family" for completion; the scheme only passes when the last step has also been reduced to an exact executable answer readout
34. when an external picture or diagram wakes naturally, use it to kill fake adjacencies and fake coexistence, but do not promote the picture itself unless it cashes back into the exact answer family
35. if a lane sounds right but its last settlement still depends on generic continuation, generic DP, or a hand-waved merge, treat that as unfinished rather than as a near-pass

### Stable Unified Scheme

The currently stabilized cross-benchmark scheme is this:

1. name the honest latent quantity
2. peel one shell
3. immediately reopen primitive competition on the thinner carrier
4. force the carrier to become one exact executable closure object
5. force local legality to upgrade into joint realizability
6. force the remaining burden to settle into one final readout, certification, or construction object
7. reject the run unless that final object belongs to the verified answer family rather than a beautiful nearby shadow

In practice this means:

- primitive use is continuous, not one-shot
- each peeled layer may wake a different primitive
- the final settlement layer is still part of the same cognition game
- a correct skeleton is not yet a pass
- a correct middle closure is not yet a pass
- a globally elegant but independently wrong family is an immediate demotion

The scheme is considered strongest when it naturally compresses into one of these exact end forms:

- one fixed-right ledger with a legal left-end interval
- one owner graph with a grounded-or-cycle witness law
- one finite reverse DP quotient
- one two-level quotient with a legal constructive pullback
- one small-vector ordered-group peel object
- one finite-state transformed automaton with a short certified prefix family
- one median or center fiber plus an exact majority or coexistence certificate

Any candidate that still ends with "then combine", "then query", "then aggregate", or
"then do DP" without naming the executable final object is still unfinished.

### Cross-Benchmark Alignment

The current package is aligned when the same scheme lands in these benchmark-specific closures:

- quarterly metadata survival:
  broad alias / family / validation stories are only costumes;
  the aligned closure is the final result-specific pinning owner `_maybe_pin_freq`

- tz-aware outer join payload survival:
  broad join / wrapping / conversion stories are only partial;
  the aligned closure is the final payload materializer `PandasIndexingAdapter.__array__`

- sliced prefetch legality rewrite:
  broad SQL or prefetch orchestration stories are only partial;
  the aligned closure is the narrow legality-rewrite helper `_filter_prefetch_queryset()`

- microbiota-AR-CD8 research significance:
  one mechanism story is not enough;
  the aligned closure must explicitly reach:
  local mechanism, nearest rival family, occupancy or concentration ceiling,
  delivery or production feasibility, selectivity or toxicity window,
  and spillover into neighboring populations

- Dynamic Instability:
  the surface object is a moving walk, but the aligned closure freezes it into:
  leaf-jump uniform-ancestor law,
  escape clock,
  root-to-node hit expectation,
  path-average or deficit quantity,
  and a depth-weighted x-side prefix rather than an unweighted one

- Min Max Subarrays II:
  broad interval pressure is only the shell;
  the aligned closure reduces every position to its unique top-min and top-max owners,
  turns witness placement into a sparse owner-edge graph,
  and closes feasibility at the component level rather than by decorated local scanning

- JOI Tour 2:
  broad path counting is only the shell;
  the aligned closure first transposes budgets into a sparse sum-kernel,
  then rewrites route histograms through root-path carriers,
  then refuses to stop at that skeleton and instead lands on one explicit settlement object:
  subtree-side upload aggregates plus one same-level settlement ledger,
  with each unordered pair closed exactly once at its LCA and only then attached to route-crossing burden

- Text editor:
  broad string-construction or BFS stories are only costumes;
  the aligned closure kills full-string state,
  keeps only prefix-length state and one doubling-validity readout,
  and settles the whole task through one prefix DP rather than a wandering string search

- Deformed Balance:
  broad grammar or parser stories are only costumes;
  A. transformed object: apply `L -> {` or `L -> }`, so the string becomes a brace walk with a designated wildcard stream;
  B. exact finite-state invariant: scan prefixes while keeping only state `(x,y)` (or an equivalent two-counter form), where `x` and `y` are the surviving finite-state balance descriptors after the transform;
  C. readout/counting: enumerate the finite prefix candidate set `X`, run the automaton over those prefix candidates, and sum exactly the candidates whose terminal state is balanced

- Onion:
  broad hull-peeling geometry is only the shell;
  support envelopes and fan-style closures can still be seductive wrong families;
  the aligned closure must instead land on the official small-vector arithmetic partition:
  one `O(sqrt n)` ordered collinear-group endpoint peel object,
  where only group endpoints plus current topmost/bottommost nonempty groups survive each layer

- AGC012F:
  broad sorted-window freedom is only costume;
  the aligned closure reverses the process,
  kills re-entry freedom,
  and lands on one duplicate-settled finite reverse-DP quotient rather than a local product law

- IOI 2020 Supertrees:
  broad matrix interpretation is only the shell;
  the aligned closure compresses nonzero connectivity into components,
  then compresses one-distance structure into maximal 1-blocks,
  and settles the task on a two-level quotient skeleton rather than the raw matrix

- AGC034F:
  broad walk dynamics are only the shell;
  the aligned closure freezes the process into one translation-invariant hitting potential,
  then spectralizes the XOR convolution,
  and lands on one nontrivial Walsh-character resolvent ledger

- hard tree layering:
  broad constructive tree stories are only costumes;
  the aligned closure compresses the burden to one per-depth one-forbidden transversal ledger,
  where previous-layer ownership is the only live residue and each layer closes by an exact distinct-choice settlement

- towns:
  broad diameter-midpoint mass balance is only a necessary shadow;
  the aligned closure must pin the true center through the diameter/median carrier,
  isolate the branching leaf set at that center,
  and settle correctness by the exact majority-component test on that leaf family rather than by branch-mass atmosphere

### What Alignment Means

This package is aligned on a benchmark when it does all of the following:

- refuses the broad but locally true explanation if a narrower final owner still exists
- refuses the moving story if a smaller static carrier already closes
- refuses a raw prefix when the true burden is weighted
- refuses one locally real mechanism if a later ceiling still decides whether it matters
- refuses to celebrate a strong middle carrier before asking whether one last owner, object, or axis still has undisposed discretion
- refuses to stop before exact interface closure
- refuses to stop at a named final object until its carried fields, merge law, and settlement boundary are all speakable
- refuses to confuse a valid decomposition with a completed settlement object
- allows reverse when reverse is the thinner honest carrier and not merely a trick
- allows web compression when local legal pieces are colliding through one global compatibility burden
- treats joint realizability as mandatory whenever many local witnesses look simultaneously legal
- treats the final closure object as part of the same cognition system rather than as a post-skill cleanup phase
- keeps primitive competition alive at every peeled layer instead of spending one early trick and coasting afterward
- requires the same primitive freedom at the final settlement layer, not only during compression
- treats final-answer equivalence as stronger than organizational neatness
- refuses to pass a closure family that is independently witnessed to be only a necessary shadow of the true answer family
- requires benchmark promotion to survive external or official-answer-family checking, not merely internal plausibility

### What Regression Looks Like

The package is drifting if future runs:

- stop at alias families, broad validators, or generic planners
- stop at producer layers instead of final authorizers or materializers
- stop at one true research mechanism without naming the limiting ceiling
- stop at a dynamic narrative without freezing the minimal static state
- stop at an unweighted prefix when the true closure is weighted
- stop at a half-closed local checker while one global countable object is still missing
- confuse missing source evidence with missing cognition and score the run as a thinking failure when the real failure was ungrounded world knowledge
- activate one good primitive near the beginning and then coast on ordinary continuation through the rest of the task
- keep using the same early primitive after the carrier has changed and a different live pressure now owns the remaining burden
- identify the right family or seam but fail to re-select primitives layer-by-layer until a final settlement object appears
- compress honestly for a while and then abandon the live primitive game at the final settlement layer
- stop at the right decomposition, DP family, or construction skeleton without naming the final query/readout/aggregation interface
- name the final object family but not the fields it carries, the way peer pieces merge, or the exact place where local burden is settled globally
- infer global realizability from many individually legal local layers without proving they can coexist
- name the right global carrier but leave one surviving endpoint / side / middle-choice / value-layer axis outside the final interface
- speak in placeholder phrases like "then answer queries" or "then aggregate globally" after the main structure is already known
- score a benchmark highly because the closure object is beautiful even though an independent witness shows it belongs to the wrong answer family
- stop at a necessary shadow of correctness, such as center-balance mass, support geometry, or local endpoint legality, when the real answer family still requires an additional certification layer



